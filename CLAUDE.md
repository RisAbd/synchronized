# synchronized — правила ведения проекта (для будущего меня при спавне)

> Этот файл автозагружается при старте сессии. Здесь — **как работать**, а не что сделано
> (что сделано и что дальше — в `docs/BACKLOG.md`). Держи оба в актуальном виде.
>
> **Веди этот файл сам.** Периодически перечитывай CLAUDE.md, вычищай устаревшее (правила,
> которые перестали быть правдой), обновляй секцию «Немедленная память» ниже. Владелец сам
> сюда почти не пишет — это моя память о текущем состоянии и договорённостях.

## Немедленная память (веду и обновляю сам — что «горит» прямо сейчас)

> Коротко: текущий фокус и свежие договорённости. Детали статусов — в `docs/BACKLOG.md`.

- **Сессия 30.07-ПРОД7 (LIVE, переключение сур + краш): ✅ БЫСТРЫЙ СВАП + УБИТ ОБРЫВ WS + ВЕРНУЛ МУСХАФ.**
  Владелец (localhost, **НЕ ngrok — вырубил, больше не поднимать**): переключился Нажм(53)→Йасин(36) —
  «долго думал ~20с», потом «переподключаюсь», сессия записалась как ДВЕ. **Диагноз по логам (падения
  со стеком НЕТ — чистый `LIVE_WS disconnect`, обрыв на loop/сетевом уровне):** (1) мой idle-deinit
  выгружал модель через 45с → старт след. суры перезагружал её (~5с заморозка event-loop, замер 5.15с)
  → «долго думал» + очередь WS переполняется/браузер рвёт; (2) в логах **ДВА `connect` на один sid без
  `disconnect`** → мёртвый коннект не реапится (`--ws-ping-interval 0`), клиент открывает новый → **два
  хэндлера гонятся за общий `st`** → «вернулось в scan». **Фиксы (коммит 1):** gen-guard в `live_ws`
  (новый коннект вытесняет старый зомби, pump/receive проверяют `st["gen"]`); idle-unload **45с→180с**
  (модель тёплая между сурами); `w2v_align.warmup()` + прелоад на connect (загрузка прячется под набор
  буфера); дроссель холодного лока `_COLD_LOCK_N=6`; `--ws-max-queue 1024`. **Быстрое переключение
  (коммит 3, владелец tg_7242 «держи основную, кандидатов не выкидывай, вес растёт → сразу свап»):**
  снят `early`-гейт (`_CROSS_MAX`) → кросс-свап активен ВЕСЬ трек (гарды целы: K-подряд лидерство другой
  суры + confirm-2 + ratio); бэк шлёт `candidates` в КАЖДОМ track-ответе, фронт держит список живым (видно
  рост веса перед свапом). **Валидация replay (match_align НЕ тронут): rec7=сура6 0 свапов; rec10 16:98→55
  рефрен не свапает; rec5 только 25 (мелькание в 26 УШЛО); rec9 1→17 за 58с; rec11 52→53; склейка
  Нажм→Йасин свап за ~8с (было 15-30).** **Мусхаф (коммит 2, tg_7171):** бэк `_surah_payload` шлёт
  `text_diyanet`, фронт — переключатель редакции Tanzil/Diyanet в панели «⚙ Шрифт» (общий ключ
  `sync.edition` с плеером). Инструменты: `work/replay_rec.py <id> [sec]`, `work/replay_cap.py`,
  `work/ws_repro.py <pcm> [sec]` (WS-клиент против живого сервера, ловит код закрытия). Всё запушено.
  **⚠️ ngrok больше НЕ использовать — владелец на localhost:8002.**
  **Корректировки по наблюдению владельца (tg_7316/7334):** (1) кандидатов на фронте показывать НЕ
  постоянно — держать в памяти, окно выбора рисовать ТОЛЬКО при детекте переключения (бэк шлёт
  `switching:true` когда cross_n≥1), и СНИЗУ (DOM: liveReader ВЫШЕ liveCands; было наоборот →
  «поверх всего»). (2) детект кандидатов быстрый (~3с), но свап текста запаздывал (ждал confirm-2) →
  **confirm-1-agree**: если k-грамм-кросс устойчиво (HOLD=2) ведёт суру И find_segments её подтверждает
  при ratio≥`_RELOC_AGREE_RATIO`=0.48 — свап сразу; склейка Нажм→Йасин 8с→~5.6с, реки целы (rec9 1→17).
  ⚠️ короткое окно `_CROSS_WIN`<20 ОТВЕРГ — ломало rec9 в суру 2 (держим полный 20с). (3) вернул
  ГРАДИЕНТНУЮ подсветку как в плеере: центр `.w.hot` + соседи `.w.lit` ±R=3 с затуханием `--wi`, hot
  чуть мягче. Крутилки `SYNC_LIVE_AGREERATIO/CROSSWIN/RELOCKHOLD`. Ждёт: дальнейший тест владельца
  (скорость свапа/подсветка — где ещё врёт).

- **Сессия 30.07-ПРОД6 (LIVE, кор-логика): ✅ УБИТ ТЕЛЕПОРТ ТРЕКЕРА НА РЕФРЕНЕ (главный баг владельца
  30.07).** Владелец на live-чтении Ар-Рахмана (localhost:8002, НЕ ngrok, НЕ режим заучивания): подсветка
  первые ~5с верна, потом улетает вперёд (55:13→55:22→55:39 при реальном чтении до 55:16). Стандарт
  владельца (tg_6976): **«любой скачок вперёд = говно, градиента нет; отставание терпимо»**. Директива
  (tg_6932/6935): это АЛГОРИТМИКА (не распознавание — он читает нераспевно, декод чистый), нужны
  **веса континуитета** (ближайший-вперёд макс вес, далеко-вперёд/назад — штраф). **Корень найден
  (`work/replay_cap.py` на `owner_vid.pcm` — реальная live-логика):** `SegmentTracker.feed` на застое
  (чтец держит рефрен «فبأي آلاء» ×31) звал **widefwd** — поиск вперёд до +600 симв. — и хватал ПОЗДНИЙ
  экземпляр аята → скачок по всей суре. **Фикс (2 дефолта в `match_align`):** (1) `_TRK_WEIGHTED=1` —
  `_match_end` берёт блок по весу=сила×приор_близости, а не «самый дальний»; (2) `_TRK_WIDEFWD=0` —
  widefwd отключён. **Замер знаковый (`work/diag_err_sign.py`): забег(>+1 аята) rec11/12/13 = 0/0/0
  (старое: до +3 вперёд = «говно»); просадка ±1-точности у весов — ТОЛЬКО отставание (терпимо).**
  Отключение widefwd регресса рек НЕ даёт (на чистом офлайн-декоде не срабатывал — реки байт-в-байт).
  **На аудио владельца (replay_cap, дефолты): плавно 55:1→55:17, на рефрене ДЕРЖИТ (55:13→55:13), телепорт
  убит** — совпадает с офлайн `traj_cap` (55:16@67с). Плюс `_track` single-feed за тик (прошлая сессия).
  Коммит запушен. Крутилки `SYNC_TRK_WEIGHTED/WIDEFWD/NEAR/FWDSCALE/BACKW/BACKSCALE/MAXFWD`. Инструменты:
  `work/replay_cap.py`, `work/diag_err_sign.py`, `work/chk_w9.py`, `work/sweep_weighted.py`, `work/traj_cap.py`,
  `owner_vid.wav/.pcm`. **Рек 15 в БД = аудио из видео владельца** (офлайн forced/w2v/google/whisper готовы —
  показать, что офлайн тайминги находятся верно, tg_6932). **⚠️ ФОКУС ТОЛЬКО LIVE + баги владельца**
  (tg_6923: реки НЕ молотить, moloti выкл). **Ждёт: тест владельца на телефоне/localhost его чтением.**
- **Сессия 30.07-ПРОД6 (LIVE-ФРОНТ): ✅ НЕПРЕРЫВНЫЙ СКРОЛЛ КАК В ПЛЕЕРЕ (владелец повторял 6-7 раз).**
  Live-фронт (`static/index.html`, модалка) перерисовывал `liveText.innerHTML` ЦЕЛИКОМ на каждый ответ
  бэка (`renderPlace`) и показывал лишь 3 аята → позиция текста на экране дёргалась, читающий терял
  место, текста впереди не видно. **Фикс:** бэк (`live_views._surah_payload`) шлёт `passage` = ВЕСЬ текст
  текущей суры, но ТОЛЬКО при СМЕНЕ суры (`st["sent_surah"]`); фронт `renderPassage` рисует суру ЦЕЛИКОМ
  один раз (стабильный DOM), `updateLive` на тик двигает только классы `.active`/`.hot`/`.near` (слово по
  `word_frac`) БЕЗ перерисовки, `liveGlide` (rAF) плавно скроллит контейнер (активный на ~32% высоты →
  видно текст впереди). Панель настроек шрифт+размер — порт плеера, ОБЩИЕ ключи `sync.arabfont/arabsize`
  (синхрон с плеером). Playwright-самотест: сура 55 (78 аятов) рендер один раз, DOM стабилен между тиками,
  `.hot` едет по словам, `.active` по аятам, glide проскроллил 1129px, 0 ошибок. `node --check`+`manage.py
  check` чисты. Запушено.
- **Сессия 30.07-ПРОД6 (LIVE init/deinit): ✅ ВЫГРУЗКА МОДЕЛИ ИЗ VRAM ПРИ ПРОСТОЕ (без рестартов).**
  Владелец: модель wav2vec2 висела в процессе `web` постоянно (~1.5ГБ VRAM), сама не выгружалась →
  чтобы дать VRAM транскрипции large-v3, я делал `docker restart web` (костыль). **Фикс:**
  `w2v_align.unload()/is_loaded()` (globals=None+gc+empty_cache; замер alloc **1270МБ→8.5МБ**). В
  `live_views` счётчик живых WS-сессий `_ACTIVE`; при падении до 0 → `_idle_deinit` через
  `_IDLE_UNLOAD_SEC=45с` (env `SYNC_LIVE_IDLE_UNLOAD`) перепроверяет счётчик и выгружает модель + чистит
  `_STREAM`. Быстрый авто-реконнект (обрыв ngrok < таймаута) поднимает счётчик → деинит отменяется,
  позиция цела. **Теперь транскрипция получает VRAM сама после простоя live — рестарт web больше НЕ
  нужен** (но при активном live + срочной транскрипции рестарт остаётся быстрым хаммером, [[gpu-first]]).
- **Сессия 30.07-ПРОД5 (WH): «МАНУАЛ 2» — разметчик повторов, v1 ЦЕЛИКОМ (коммиты c706270/ae3be8d/939cd0c/52a3247).**
  Облегчённый ручной элайнер: отмечаешь ТОЛЬКО повторы (что/сколько раз) → тайминги ставит
  `forced_align(slots=)`. Ядро `pipeline.slots_from_marks`+`flat_range_words` (разметка `[{start,end,count}]`
  по плоским индексам диапазона → слоты, юнит 13/13). Плагин `sources/marks.py` (SELECTABLE/AUTO=False/
  ISOLATE, читает `rec_dir/marks.json`, подхватывается общим `run_one`). Эндпоинт `POST /r/<id>/marks/save`
  (`views.marks_save`, валидирует до GPU) + роут `/r/<id>/marks`. Фронт `static/mark_repeats.html` (клик=span,
  счётчик ×N, чипы, блок пересечений) + ссылка «🔁 повторы» в плеере (?debug=1). Полный HTTP e2e (playwright
  POST→worker) на rec7: готово, coverage 0.924, fj=0, reps=5; rec7 очищен. Остаток — UX-итерации с владельцем.
- **Сессия 30.07-ПРОД4 (WI): РЕЖИМ ЗАУЧИВАНИЯ v0 (опт-ин, коммит 04f3acb) — killer-фича (tg_4810).**
  Читаешь по памяти, отклонился → КРАСНЫМ + звук. `SegmentTracker.quality`=coverage(minblk=3) свежего
  декода к ожидаемому окну корпуса; детектор в `live_views._analyze` (гистерезис `_MEM_LOW=0.06`/
  `_MEM_HOLD=5`/`_MEM_OK=0.25`); флаг по WS `memorize:1/0`; фронт — чекбокс «🧠 режим заучивания» + красная
  подсветка + `beep()`. **⚠️ ПРЕДЕЛ (калибровка `work/probe_memorize.py`, 6 МЕЛОДИЧНЫХ рек):** верное
  МЕЛОДИЧНОЕ чтение всё равно 4-9% ложно краснит (rec9 15 эпизодов) — бедный мелодичный декод (как
  рефрены/дрейф). Потому ОПТ-ИН + строгий порог. Мелодика = ХУДШИЙ случай; заучивание = ПРОСТОЕ чтение
  → декод чище → ложных меньше. **TP-валидация + финальная калибровка LOW/HOLD ждут теста владельца на
  ЕГО простом чтении с телефона** (офлайн записи нет). Крутилки `SYNC_LIVE_MEMLOW/MEMOK/MEMHOLD`.
- **⚙️ ИНФРА (30.07, указка владельца «поднимай ОДИН сервис»): `web` и `live` СЛИТЫ в один ASGI-сервис.**
  `web` теперь = `uvicorn synchronized.asgi:application` и отдаёт ВСЁ: обычный Django (HTTP/статика/БД)
  И websocket `/live/ws` (роутер `asgi.py`: `/live/ws`→`live_ws`, остальное→Django). Отдельного `live`
  БОЛЬШЕ НЕТ (сносить `docker compose up -d --remove-orphans`). Сервисы: `redis / web / worker`. `web`
  требует GPU (live-декод wav2vec2). **Порт:** в РЕПО стандартный 8000; ЛОКАЛЬНО — 8002 через gitignored
  `docker-compose.override.yml` (`ports: !override ["8002:8000"]`; 8000 занят чужими проектами — memory
  [[local-web-port-8002]]). Плеер И live-микрофон — на ОДНОМ порту (локально 8002, ngrok → 8002). ⚠️
  uvicorn БЕЗ --reload → после правок live_views/asgi/views: `docker compose restart web` (НЕ `live`).
- **Сессия 30.07 (WI, кор-логика): (1) фикс ХОЛОДНОГО мимо-лока + (2) найден отдельный баг прыжков.**
  Владелец усомнился, что его аудио вообще норм → прогнал его записи через large-v3: **чистейший
  Ар-Рахман** (`work/live_cap_*.truth.txt`), не мусор. **Корень мимо-лока:** запись начинается с
  БАСМАЛЫ (одинакова у всех сур) → первые ~6с неоднозначны → холодный лок встаёт не в ту суру (29
  вместо 55), а трекер фейкает движение по чужому корпусу (застоя нет → старый перелок молчит).
  **Фикс (`live_views._analyze` track-ветка):** фоновый scan во время track (по УЖЕ декодир. окну,
  без доп. GPU); если ДРУГАЯ сура — #1 подряд `_RELOCK_HOLD=3` тика в РАННЕМ окне `_CROSS_MAX=40`
  тиков после лока (`lock_n`) → быстрый кросс-сура перелок. Ар-Рахман: выправление t=14→**t=10**;
  на «хорошей» записи и на рефрене не триггерит (лидер=та же сура); rec7 не ухудшил. Раннее окно —
  чтобы НЕ прыгать на мелодике позже (там доверяем последовательности, смену суры ловит застой).
  Крутилки `SYNC_LIVE_RELOCKCONF/RELOCKHOLD/CROSSMAX`. **(2) ✅ ПОЧИНЕН БАГ ПРЫГАНЬЯ ПО МУСОРНЫМ СУРАМ
  (был и в baseline): на мелодичном хвосте с перечитками (rec7, сура 6) СТАРЫЙ застой-перелок прыгал по
  случайным сурам (108/18/45/2…) — find_segments по шумному 20с-окну возвращает мусорную суру, а гард
  `v2[0][0]!=cur_surah` её пропускал. ФИКС: `_passage_ratio` — свитчим корпус ТОЛЬКО если новый пассаж
  реально объясняет декод (skeleton-difflib ≥ `_RELOC_RATIO_MIN=0.45`). Замер: мусорные суры rec7
  ratio 0.24–0.40 (блок), легитимный Ар-Рахман 29→55 ratio 0.75 (проходит). Итог: rec7 держится в
  суре 6 ВСЕ 550 тиков (0 прыжков, было — скачки по 8+ сурам); Ар-Рахман фикс цел. **Валидация на
  rec9 (мульти-сегмент Фатиха+Исра) vs TRUE-baseline (git 7ff8002, дореформенный): baseline прыгал
  по 15+ сурам (17,1,6,4,9,5,7,27,72,30,20,18,16,10…) — хаос; с фиксом — 5 сур, доминируют ВЕРНЫЕ
  17+1 (272 тика).** Крупное анти-прыганье, регресса нет.
- **Сессия 30.07-ПРОД3 (WI, кор-логика): растущий cold-аккумулятор на холодный лок (робастность, НЕ
  скорость) — остаток (а) ЗАКРЫТ.** Растущий буфер (`_COLD_GROW=45с`): роллинг капнут на 20с, но
  find_segments по буферу ОТ НАЧАЛА на длинном окне давит мусорную суру (не держит match 30-45с). Копим
  всё с начала до 45с ТОЛЬКО в scan, на лок декодим его вместо роллинга, при track освобождаем. Изолировано:
  реки, что лочатся ДО 20с-капа (rec5/7/10/11/12, t=7..14с) — растущий==роллинг → байт-в-байт baseline;
  затронут только rec9. **rec9: устойчивый мусор An-Naml (сура 27, ratio 0.43 — 0.02 под гейтом 0.45!)
  УСТРАНЁН с n=141** (baseline мешал 27 на n=141/147/153 с Фатихой); strong-лок Фатихи t=24с (было t=23),
  захода в 27 нет. **⚠️ COLD-CONFIRM (лок при ratio≥floor если суру подтвердили N раз) ПРОБОВАН и ОТВЕРГНУТ:**
  мусор в окне <20с (растущий==роллинг, не задавлен) копит счётчик → floor 0.40 залочил rec9 в суру 27 на
  t=18с (мислок, проверено). Сура-мусор rec9 (0.43) в ТОЙ ЖЕ полосе, что верные пассажи rec5/12 (0.40-0.43)
  — ratio не разделяет, порог понижать нельзя. Safe-ускорения холодного лока НЕТ: узкое место = ratio-гейт
  + разбавление басмалой (короткая Фатиха), не длина буфера. Крутилка `SYNC_LIVE_COLDGROW` (0 → выкл).
  Остаток WI: (б) rec5/12 мелькание в соседнюю суру — калибровка слухом; режим заучивания; «живые кандидаты в track».
- **Сессия 30.07-ПРОД2 (WI, кор-логика): ✅ ПОЧИНЕН МУЛЬТИ-СЕГМЕНТ дрейф Фатиха→Исра (rec9).** Корень
  оказался НЕ в холодном локе и НЕ в линейном корпусе, а в СТ​АЛЛ-ПЕРЕЛОКЕ на ПЕРЕХОДЕ между несмежными
  пассажами: буфер = хвост Фатихи + начало Исры + такбир/пауза → `find_segments` мелькает МУСОРНОЙ сурой
  (rec9: 2, 23, 4, 35, 21…, каждый релок другая), а ВЕРНАЯ (17) держится ПОДРЯД. Старый код (а) переключался
  на ПЕРВЫЙ же `find_segments` с ratio≥0.45 → хватал мусор (дрейф 1→2→23), (б) блок `len(v2)>=2` ОТВЕРГАЛ
  верную суру, когда на переходе она возвращалась ОДНОАЯТНЫМ коротким пассажом (len<2) → 17 бралась только
  к t=222с. **ФИКС (`live_views._analyze` stall+cross):** (1) снят `len(v2)>=2` (ratio-гейт и так защищает
  от мусора; на переходе верная сура приходит ОДНОАЯТНЫМ пассажом len<2 и раньше отвергалась до t=222с);
  (2) **ЕДИНЫЙ confirm-2 для ЛЮБОГО перелока (cross И stall):** новую суру берём, только если тот же
  кандидат подтвердился `_RELOC_CONFIRM=2` релок-попытки подряд; одноразовый ложный кандидат (мусор
  перехода / интро-истиаза 16:98 / соврaвший cross-лидер) счётчик не копит → отсекается.
  `_build_corpus` продолжает корпус ТОЛЬКО в пределах суры последнего сегмента.
  ⚠️ v1 (первый коммит 34667b1) имел strong-путь `ratio≥0.6` + cross-освобождение — ОТКАЧЕНЫ: strong
  впускал одноразовую истиазу (rec12 прыгал в 16), cross-лидер врал (rec5 прыгал в 26); rec9-фикс держится
  на чистом confirm-2 (там ratio 0.52<0.6, strong не участвовал).
  **Замер (`work/replay_rec.py <id> [max_sec]` — WS-логика на любой реке из media):** rec9 baseline
  `1→2→23→17(t=222с)`, 4 суры → фикс **`1→17(t=66с)`, 2 суры, дрейф в 2/23 УБРАН** (единственное переключение:
  stall n=438 cand=17×2 conf=True). **Регресс проверен на 6 реках vs baseline b2ad1c0:** rec7 (сура 6)
  0 прыжков ✓; rec10 (Ар-Рахман) `16→55(t=36с)` рефрен ×31 не прыгает ✓; rec11 `52→53` ✓; **rec5
  `25→26→25` и rec12 `56→57→56` — БАЙТ-В-БАЙТ как baseline (мелькание в СОСЕДНЮЮ суру Фуркан→Шуара/
  Вакиа→Хадид — свойство записей, НЕ мой регресс; идёт через корпус, не через confirm-релок).** Крутилка
  `SYNC_LIVE_RELOCCONFIRM`; лог перелоков пишет `cand=<сура>×<n> conf=<bool>`.
  **Остаток (глубже): (а) rec9 0–66с чтец в Фатихе виден верно лишь после релока (холодный лок раннего
  окна 12.6с промахивается в 19, гейт отвергает ✓, лок в Фатиху на t=23) — ускорить find_segments по
  РАСТУЩЕМУ буферу. (б) мелькание в соседнюю суру rec5/rec12 (26/57) — легитимный переход или перечитка?
  калибровка = слух владельца (как reps/дрейф).**
  Крутилка `SYNC_LIVE_RELOCRATIO`; отладка перелоков — env `SYNC_LIVE_DBG=1`. Инструменты: `work/replay_rec.py`
  (реки из media), `work/replay_cap.py` (WS-памп live_cap владельца, без HTTP-гейта — истинное поведение
  live; `stream_selftest` бьёт HTTP-путь с багом гейта _PROC_STEP, для регресса НЕ показателен).
- **Сессия 19-ПРОД7 (29.07): LIVE-СТРИМИНГ WI довёл до рабочего по итеративному фидбеку владельца
  (десятки голосовых, тестит удалённо с телефона через ngrok).** Ядро — `service/recitations/live_views.py`
  `live_stream` (эндпоинт `POST /live/stream`; сервис — единый `web`-ASGI, см. заметку ИНФРА выше,
  раньше был отдельный `live` :8010). **Архитектура (роллинг-буфер PCM):** фронт (`static/index.html`, Web Audio
  ScriptProcessor) шлёт сырой Int16 PCM 16кГц крохотными чанками (SEND_MS=100мс, `?pcm=1` octet-stream,
  без ffmpeg/webm) → сервер копит роллинг-буфер `_BUF_CAP=20с`, развязка приём/обработка (append 1-3мс,
  тяжёлый декод не чаще `_PROC_STEP=0.5с`). **Фаза scan:** k-грамм-голоса с затуханием (`_DECAY=0.6`) →
  `match_align.topk_from_votes` → список кандидатов-мест с % (показ с ~3с, лидер всплывает по
  нарастающей, лок в track на ~8с). **Фаза track:** декод последних `_TRACK_WIN=8с` → `SegmentTracker`
  ведёт позицию вперёд; показываем prev/cur/next (3 аята, длинные обрезаны `_TRUNC=120`) + пословная
  подсветка (`word_frac` из позиции трекера). Тап по кандидату → `?boost=surah:ayah` (голоса +3,
  затухают). Перелок find_segments — ТОЛЬКО при долгом застое (`_RELOC_STALL=10` + кулдаун
  `_RELOC_COOLDOWN=20` тиков), иначе тормозил (был баг: find_segments по растущему буферу каждые 4 тика
  → латентность росла 0.6→2.4с → зависание к 40с; ПОЧИНЕНО буфером 20с + редким перелоком, латентность
  плоская ~200мс). **Само-тест:** `work/stream_selftest.py` (реальный эндпоинт vs истина w2v): rec5=1.00,
  rec7=0.93, rec12=0.89, rec9 (мульти-сегм Фатиха+Исра) 0.01→0.66 перелоком; латентность медиана ~450мс.
  **⚡ ВЕБСОКЕТ (фикс зависания, tg_5558):** HTTP-версия виснула — 100мс-чанки=10 POST/с, ngrok-free
  троттлит и рвёт коннект на ~12с (воспроизведено: HTTP умирал на 12с, ws шёл 40с без обрывов). Перевёл
  на вебсокет: **live-сервис теперь `uvicorn` (ASGI), НЕ runserver** — `service/synchronized/asgi.py`
  тонкий роутер (`websocket /live/ws`→`live_views.live_ws`, HTTP→Django), `uvicorn`+`websockets` в
  `work/pydeps` (pip --target), команда в docker-compose. Ядро вынесено в `_process_pcm` (общее для
  HTTP `live_stream` и WS), GPU-декод в `run_in_executor`. Фронт: WebSocket, кадры PCM Int16 потоком,
  boost/reset — текст. ⚠️ uvicorn БЕЗ --reload → после правки live_views/asgi делать
  `docker compose restart web` (сервис слит, см. заметку ИНФРА выше). Шрифт/размер live подцепляются
  из плеера (`sync.arabfont/arabsize`).
  Инструменты: `work/proto_stream*.py`, `work/proto_coldlock.py`, `work/stream_selftest.py`, `work/ws_test*.py`.
  match_align: `ayah_density`/`topk_from_votes`/`locate`/`StreamLocator`/`SegmentTracker`. Env-крутилки
  `SYNC_LIVE_*`. **Открыто (владелец продолжит гонять):** вебсокет (пока частые POST — я показал, что узкое
  место = GPU-декод, не сеть; ws почти не ускорит); рефрен-суры (rec10 предел мелодичного декода); режим
  заучивания (красное отклонение); «всегда живые кандидаты в track» (k-грамм на мелодике шумит → пока
  кандидаты только в scan, смена пассажа ловится застоем трекера).
- **⚠️ GPU-ДИСЦИПЛИНА (владелец ЗОЛ, tg_5462/5464/5470/5522, memory [[gpu-first]]):** транскрипция его
  голосов (audio-task) = **ВСЕГДА GPU + ВСЕГДА large-v3**, НИКОГДА CPU, НИКОГДА малая модель. 6ГБ карта:
  web-сервис (wav2vec2 live ~1.5ГБ) + large-v3 (~4ГБ) не влезают → при OOM **освобождать VRAM**
  `docker compose restart web` (не CPU!). Его сообщения — приоритет, live можно ронять. Перед
  транскрипцией: `nvidia-smi`; если VRAM>500МБ → рестарт web. TTS (Silero) тоже на GPU.
- **⚠️ ОЗВУЧКА владельцу (memory [[owner-audio-sendvoice]]) — ГОТОВ СКИЛЛ `tts-voice` (tg_5748):**
  отчёты/объяснения — ГОЛОСОМ через Telegram `sendVoice` (пузырь!), НЕ `sendfile`/документом.
  Переиспользуемый скилл `~/.claude/skills/tts-voice/` (Silero v4_ru на GPU → .ogg/opus → sendVoice,
  батчи для длинного текста внутри): `/usr/bin/python3.12 ~/.claude/skills/tts-voice/tts.py --text "…"
  --send-telegram me --bot 2 --caption "… ngrok: <url>"`. Голос **мужской**, дефолт **`eugene`**
  (tg_5749); женские baya/kseniya/xenia. Хост-python3.12 (torch+cuda), omegaconf в `pydeps` скилла.
  Каждый раз прикладывать ngrok в caption. Токен бота 2 — `~/.claude/remote-control-bot/.env`
  `TELEGRAM_BOT_TOKEN_2`. (Старый разовый `work/tts_msg.py` заменён скиллом.)

- **Сессия 19-ПРОД6 (28.07): АВТО-СТРУКТУРА ПОВТОРОВ (оракул) ВСТРОЕНА + все 9 на github.io прогоном
  `w2vo` (BACKLOG WG).** Владелец снял паузу («фигачь до талова, не останавливайся, сам остановлю»).
  Сделал `w2v_align.greedy_repeat_slots` — ЛОКАЛЬНЫЙ оракул правдоподобия (H0×m vs H1×m+1 по CTC
  path_score в ОКНЕ кадров вокруг фразы → дёшево, масштаб на 9; глобальный proto_greedy был бы часами
  на rec9 T=64k). Гейт «переросших» аятов = аномально длинное СЛОВО (спан/букву > 2.8×медианы —
  перечитка всегда растягивает спан; зоны повтора ×5-8, спокойные ×1-2 → селективно+быстро). Порогов на
  ЧИСЛО повторов нет, margin=3. Слоты → `forced_align(slots=)`. **Валидация все 9 офлайн по кэшу эмиссий
  (`work/greedy_validate.py`): fj=0 ВЕЗДЕ, cov 0.998-1.0**; reps 23/27/41/98/8/51/21/7/12 (оракул берёт
  БОЛЬШЕ фраз-перечиток, чем акустич. Viterbi). rec7 ≈ эталон test2 (28/33; السماوات والأرض/لا إله إلا هو
  восстановлены). **Интеграция без регрессий:** флаг `SYNC_W2V_REPEATS` (viterbi=дефолт/oracle) в
  `sources/w2v.py` + отдельный плагин `sources/w2vo.py` (PRIORITY=15>w2v → дефолтом НЕ становится). Все 9
  заполнены `w2vo` офлайн (`work/populate_w2vo.py`), залиты на github.io (`r/<id>/w2vo.json`, дефолт
  data.json НЕ тронут). **ЖДЁТ СЛУХ ВЛАДЕЛЬЦА** (rep-точность только ухом) — сравнить w2v vs w2vo, решить
  дефолт. Остаток: низко-ΔH хвост margin=3 (вкл. ложные?), قنوان/انظروا = акустич.предел (нужен текст-приор).
  **НОВЫЙ КРУПНЫЙ ПУНКТ (владелец tg_4810, BACKLOG WI):** live-распознавание в реальном времени +
  быстрый КОНТЕКСТНО-зависимый поиск по Корану (уверенность + контекст текущей суры для разрешения
  неоднозначных мест; killer: live-подсветка, «память рядом», режим заучивания с красной подсветкой
  отклонений). Тайминги вторичны, важна скорость+уверенный поиск. difflib «тормознутый» → можно поднять
  приоритет. Инструменты сессии: `work/greedy_validate.py`, `work/diag_gate.py`, `work/populate_w2vo.py`.
  **WI НАЧАТ — ядро быстрого поиска сделано (ресёрч `docs/RESEARCH-live-search.md`):** difflib-поиск
  тормозил (rec9 20с). Сделал в `match_align`: (1) `locate()`/`StreamLocator` — k-грамм-голосование по
  окну O(окна), ~0.2мс (в 100000× быстрее), для холодного поиска; (2) **`SegmentTracker` (ГЛАВНОЕ)** —
  онлайн-указатель по известному пассажу (find_segments задаёт → локальный difflib в узком окне мини-
  корпуса ведёт позицию): **7 из 9 рек точность ±1 аят 0.90-1.00** (rec5/7=1.00, rec12=0.99; мульти-
  сегмент Фатиха+Исра rec9 0→0.90). Открытая проблема — рефрен-суры (rec10 Ар-Рахман ×31 → 0.07,
  rec14 0.49): плотный рефрен + мелодичный декод; CTC-рескоринг НЕ поможет (рефрены звучат одинаково).
  Инструменты: `work/proto_locate.py`, `work/proto_online.py`. След.: стрим-ASR (сейчас симуляция полным
  декодом), рефрен-робастность, режим заучивания. ⚠️ Все эксперименты трекинга (band/анти-залипание/
  липкий ре-лок) сверх baseline проверены и ОТКАЧЕНЫ — в проде только чистые locate/StreamLocator/SegmentTracker.

- **Сессия 19-ПРОД5 (26.07): МУЛЬТИ-СЕГМЕНТНЫЙ поиск Корана — РЕШЕНО (BACKLOG WF, коммит `aba6e51`).**
  Владелец на rec9 нашёл регрессию: свежий w2v потерял Аль-Фатиху (читается в начале перед Аль-Исрой) →
  подсветка глючила. Причина: независимый w2v `find_range` искал ОДИН непрерывный диапазон (google/
  whisper/forced держали Фатиху, т.к. матчат текст пословно → несмежные суры ОК). Директива владельца
  (много голосовых, был раздражён «не тупи»): **аудио НИКОГДА не один непрерывный кусок** (записи с
  намаза — Фатиха + основная сура + такбиры; чтецы повторяются) → ОДИН общий source-agnostic алгоритм
  должен находить ВСЕ места по похожести текста. **Сделал `match_align.find_segments`:** primary
  find_range → отшелушить непокрытый префикс/суффикс декода → рекурсивно тем же find_range. Гарды
  вторичного сегмента (primary без порога): ratio≥0.20 + matched≥45 + ≥1 непрерывный блок ≥8 симв →
  отсекает истиаза 16:98/басмалу/такбиры/дуа 4:81-82 (big=0 или matched<45), держит Фатиху (matched~79).
  w2v.run зовёт find_segments. **Все 9 перегнаны на GPU, залиты на github.io, live проверен:** rec9 =
  `1:1-7, 17:1-60` (Фатиха @0.5с как forced), rec5/6/7/10/11/12/13/14 — свои диапазоны без ложных
  сегментов, fj=0. Инструменты: `work/proto_segments.py`, `work/diag12.py`, `work/t_seg*.py`,
  `work/regen_w2v_one.py`. Эмиссии офлайн: `work/rec{7,9,10,12,14}_w2v_emis.npy`. **Такбиры НЕ
  детектятся (верно, владелец просил).** Остаток: гард мог бы быть строговат для редкого короткого
  истинного 2-го чтения — калибровать по данным при появлении.

- **Сессия 19-ПРОД4 (25.07): ПАУЗА до выходных (владелец, tg_4559/4564: «стоп только на СЕГОДНЯ,
  крест не ставь» — глубокий авто-детект нужен, но долго/нудно/регрессии, не на коленке).** Итоги дня:
  • **test2 (эталон rec7) ПОЧИНЕН** — единственный сбой был в зоне 101: text-версия (`rec7_plan.txt`)
    задублировала وخلق كل شيء ×4, а чтец читал ×3 (ручной + акустика подтвердили) → лишний 4-й проход
    съедал время وهو → на 3:27 подсветка висела на وخلق. Заменил слот-подпоследовательность 101 на
    легальную из ручного (`work/fix_test2_101.py`), пере-forced → **وهو@208.2, fj=0**. Остальное test2
    (انظروا×3 и пр.) не тронуто. test2 остаётся ЭТАЛОНОМ.
  • **`test` (ТЕСТ 1 → метка «ручной+авто-время»):** структура РУЧНОГО эталона + авто-тайминги forced
    (`work/slots_from_manual.py`, 202 слота, cov=1.0, fj=0). Владелец: его ручной лучше по структуре,
    но нужны авто-тайминги, не сырые — это оно.
  • **github.io: залиты ВСЕ прогоны ВСЕХ рек** (вкл. manual/test/test2), тыкаются на статике. `build_ghio`
    пишет пофайлово `r/<id>/<key>.json` на каждый ready-run + `data.json`=дефолт; `player.html`
    `dataCandidates()` — при `?asr=` сперва статичный `<key>.json`, затем `data.json?asr=` (живой Django
    404→фолбэк). Все 9 рек live 200. Коммиты `71c18c8`/`ae07232`.
  • **🟢 ГЛАВНАЯ НАХОДКА — авто-структура повторов через ОРАКУЛ правдоподобия (BACKLOG WG):** forced-Viterbi
    сам судит H0(фраза×1) vs H1(×2) по `path_score`; ΔH>0 ⟺ второе звучание реально. **Порогов нет.**
    rec7: فраз-перечитки приняты (**بديع السماوات والأرض +26.7 — место 6 recovered несмотря на ب→ق!**,
    وخلق +30.5, والزيتون +37.2), ВСЕ مادّ-холды отвергнуты (حسبانا/ماء/طلعها/يصفون). Остаток: одиночные
    мутные слова قنوان/انظروا оракул отвергает (−13.7/−18.6, модель не видит ن-ظ-ر — акустич.предел,
    нужен текст-приор). Инструменты: `work/proto_oracle.py`/`proto_oracle_local.py`/`proto_greedy.py`
    (жадный итеративный поиск, план владельца tg_4539). **СЛЕДУЮЩЕЕ (не сегодня): встроить оракул-жадный
    поиск в `sources/w2v.run()` + валидация на всех 9 без регрессий (ОТДЕЛЬНЫМ заходом, не вслепую).**
  • **🔵 Идея владельца «Мануал 2» (BACKLOG WH):** облегчённый ручной элайнер — отмечать ТОЛЬКО повторы
    (что/сколько раз) → `forced_align(slots=)` → почти идеал; проще manual v3. Кандидат в отдельную статику.
  • **Дальше (следующие сессии):** переходить на ДРУГИЕ реки (rec7 изучен досконально); владелец
    прослушает и даст разметку повторов/текст (инструментов теперь больше). ⚠️ `rec7_plan.txt`,
    `rec7_w2v_emis.npy` — не удалять.

- **Сессия 19-ПРОД3 (25.07): ЭТАЛОН rec7 СОБРАН в `test2` (forced-Viterbi по РУЧНОМУ тексту повторов
  владельца). СЛЕДУЮЩЕЕ ГЛАВНОЕ — переписать w2v-возвраты, чтобы авто-выдача ≈ этому идеалу.**
  Владелец дал расширенный текст (`rec7_plan.txt`, корень репо: канон 6:95-103 + продублированные
  повторы как читал чтец) → инструмент `work/forced_from_text.py` мапит текст на канон-позиции
  (surah:ayah:wi) + гонит `w2v_align.forced_align(slots=)` по акустике → кладёт прогон под ключ.
  • **Источники-слоты `test`/`test2`** (`sources/test.py`,`test2.py`, коммит `6ab199e`) — ALIGNED,
    AUTO/SELECTABLE=False, заполняю скриптом (`SAVE=test env` → `pipeline.build_manual_run`). Для
    опытов, тыкаются в плеере ОТДЕЛЬНО от w2v. `test`=v1, `test2`=ИДЕАЛ.
  • **Маппер lookahead:** слово в диапазоне бывает МНОГО раз (وهو/الأبصار/شيء) → среди канон-позиций
    того же скелета выбор по (1) продолжаемости фразы вперёд (canon[q+1]==next_token), (2) близости к
    p. Починил хвост 6:103: второе «وهو يدرك الأبصار» → 3,4,5 (было 6,4,5 — «похоже но другое»).
  • **test2 итог:** 200 слов, cov=1.0, **fj=0**, 40 повторов, 0 не сматчено; بديع-перечитка@177.5,
    انظروا×3, قنوان×2; порядок vs ручной **0.975** (test 0.970, w2v 0.957). «Лишний» 3-й возврат
    وخلق(101:11) — ВЕРНЕЕ ручного (в тексте وخلق ×4, в ручном отмечено ×2). Владелец: test2 = эталон.
  • **🔵 ПЛАН (озвучен владельцем):** из test2 (идеал) переписать w2v repeat-aware Viterbi, чтобы
    авто-детект давал ≈ этот результат без ручного текста. Ключевое отличие идеала от текущего авто:
    авто теряет بديع-перечитку (ب слышится как ق) — идеал её берёт, т.к. структура задана. Значит w2v
    надо доучить брать возврат туда, где акустика слабая, но текстово он ожидаем (или иной механизм).
  Инструменты: `work/forced_from_text.py`(маппер+forced+save), `work/diag_test_run.py`, `tools/
  compare_manual.py -e AKEY=test2`. ⚠️ `rec7_plan.txt` — эталонный текст владельца (не удалять).

- **Сессия 19-ПРОД2 (25.07): ВЛАДЕЛЕЦ РАЗМЕТИЛ РУЧНОЙ ЭТАЛОН rec7 → сверка + его идея слотов
  ПОДТВЕРЖДЕНА данными. Плюс капитальная переделка ручного элайнера по его фидбеку.** Всё в main.
  • **`compare_manual` (эталон manual vs авто-w2v, rec7):** порядок 0.957; возвраты **9/9 matched,
    recall 1.0, precision 1.0** (все возвраты владельца авто поймал, ложных нет). Единственное
    расхождение слова-приземления — **место 6: эталон возврат на بديع(101:0), авто на السماوات(101:1)**
    → эталон ПОДТВЕРДИЛ мой диагноз недобора (ب слышится как ق). Запуск: `docker compose exec -T
    -e REC=7 -e AKEY=w2v worker python - < tools/compare_manual.py`.
  • **Идея владельца tg_4343 (дать расширенный текст с повторами → монотонный Viterbi) — ПРОВЕРЕНА
    на его же ручном:** взял структуру повторов ручного прогона (202 слова/43 повтора) как СЛОТЫ →
    `w2v_align.forced_align(E,...,slots=...)` → **cov=1.0, fj=0, все 43 повтора разложены, МЕСТО 6
    ПОЧИНИЛОСЬ** (بديع в перечитке встало на 177.99с — Viterbi ОБЯЗАН разложить заданное слово,
    двусмысленность ب/ق не мешает). Вывод: «ручная структура + forced-timing» > авто-детект (точные
    онсеты + не теряет بديع), цена — ручная разметка. Механизм `slots` СОХРАНЁН (не удалять!).
    Инструмент `work/slots_from_manual.py`. **🔵 Предложил владельцу сделать это отдельным прогоном
    (ручной→forced-timing) — ждёт его «да».**
  • **Ручной элайнер (`manual.html`) — переделка по фидбеку:** (1) откат Ctrl+←/→ = аудио −3с, запись
    до −2с (позже стереть), 1с грейс (не пишем — подвести фокус); (2) склейка смежных интервалов
    одного слова (не плодить дубль-спан на возврате; перечитка не задета — там чужое слово между);
    (3) залипшие зелёные `.rec` чистятся; (4) клик по нативному плееру = тот же возврат на метку;
    (5) тумблер маркера-гида `◎ гид-прогон` (авто-позиция мешала — убегает); (6) размер шрифта A−/A+;
    (7) громкость в localStorage. Стрелки под RTL (←вперёд), скорость мин 0.25 (0.1 убран). Коммиты
    `84997a2`/`9a8daf8` и др. Node-syntax чист. Гид объяснён владельцу: жёлтый = где АЛГОРИТМ считает
    чтеца (сверка на слух), независим от зелёного (твой эталон).

- **Сессия 19-ПРОД (25.07, после компакта): владелец оспорил «8/8» — «ты сам 14 насчитывал».
  ЧЕСТНЫЙ ре-аудит всех 14 вердиктов (`work/owner_ear_verdicts_s18.md`) по свежим эмиссиям.**
  «8/8» был подмножеством из тест-скрипта — инфляция, признал. Реальная картина по 14 местам
  (repeat-aware Viterbi чинит ПЕРЕЧИТКИ, но НЕ дрейф):
  • **Починено repeat-aware:** خضرا BACK (возврат на فأخرجنا, не перелёт), الزيتون والرمان (перечитка
    проигрывается), صاحبة (span не растянут 5с), КОНЕЦ — عليم подсвечивается + подсветка НЕ убегает на
    ذلكم + لا تدركه/وهو يدرك перечитываются (владелец подтвердил «конец получше»).
  • **Место 6 (بديع السماوات والأرض) — АКУСТИЧЕСКИЙ ПРЕДЕЛ, НЕ баг R/P:** чтец реально перечитывает
    بديع @177с (декод «قديعر»), но модель слышит начальный **ب как ق** (P(ب)~0.000–0.003, декод=قاف)
    → путь через буквы ب-د-ي-ع стоит дорого → DP честно возвращается на السماوات (недобор на 1 слово).
    Форсить = замазка. Тот же класс, что места 6/10/11/14 старой диагностики. Инструмент `work/probe_place6.py`.
  • **Дрейф (место 3 отставание, место 5 обгон) — НЕ перечитка, repeat-aware не трогает.** Ось B,
    абсолютного порога нет (4 подхода отвергнуты), остаётся относительный детектор (дорого, калибровка слухом).
  **ФРОНТ:** полоска прогресса перенесена ВНУТРЬ слова (`bottom:2px`, было `-2px` снаружи), высота
  2px→4px (владелец: «внутри самого слова + чуть толще»). Коммит `3d2c914`. **Эмиссии rec7 пере-сохранены**
  (`work/rec7_w2v_emis.npy` свежий; ⚠️ `emissions()` даёт stride в МС — при декоде t(с)/(stride/1000)).
  Инструменты: `work/audit14_rec7.py`, `work/decode_rec7_zones.py`, `work/probe_place6.py`.
  **🔵 СЛЕДУЮЩЕЕ:** ждать слух владельца (перечитки доведены до предела акустики). Крупный остаток —
  ДРЕЙФ (места 3/5): относительный детектор перерастяжения (5-й подход, прототип `proto_drift_relative.py`),
  калибровка = слух. Другие реки НЕ трогать (tg_4059) до подтверждения rec7. Ниже — исходный блок сессии 19:

- **Сессия 19 (25.07, tg_3937..4101): ВОЗВРАТЫ ПЕРЕДЕЛАНЫ по директиве владельца — repeat-aware
  CTC-Viterbi (один проход, аллайнер сам ходит назад), rec7 8/8 мест. Всё в main.** Владелец голосом
  раскритиковал (а) мой ДВУХПРОХОДНЫЙ подход (детект кусков→дублирование эталона→2-й Viterbi, tg_3950
  «первый проход — тот же ломаный Viterbi»), (б) пред-детект с жёсткими порогами (tg_4059 «убери все
  ограничения»), (в) предложил текстовый диф по сырому декоду (tg_4053). **Сделано:** (1) проверил
  текст-диф ДАННЫМИ (`work/proto_selfsim.py`) — сырой декод ~1 буква/слово, само-похожесть difflib
  r≈0.58 и на перечитках, и на шуме → НЕ разделяет; отверг обоснованно. (2) **repeat-aware Viterbi**
  (`w2v_align._ctc_viterbi_repeats`+`repeat_align`, `sources/w2v.run`→него): к stay/+1/+2 добавлен
  ПРЫЖОК-НАЗАД (конец слова w→начало раннего w', окно R=9, штраф P). Возврат нативен за 1 проход,
  прыжки вперёд структурно невозможны, время монотонно → ложняк не берётся акустикой. **P=0 (штраф на
  возврат ДУШИЛ реальные перечитки — владелец был прав, tg_4087).** rec7 live GPU: **8/8 мест владельца
  fj=0 cov=1.0** (خضرا/الرمان/السماوات/صاحبة/عليم/ذلكم/وكيل/وهو + перечитка لا إله إلا). (3) **фронт:
  жёлтая полоска прогресса под словом** (`.w.hot::after`, tg_4059 — не заливка/«моргающий светофор»).
  Playwright: возвраты рисуются (205с→وخلق, 120с→أعناب, 244с→الأبصار), bar 2px #ffd24a. Memory
  [[repeat-aware-viterbi]]. **✅ ЧИСТКА СДЕЛАНА** (коммит `344c64c`): `w2v_repeats.py` удалён целиком,
  двухпроходные хелперы (`align_score`/`slots_from_verses`) убраны; `forced_align` оставлен как чистый
  монотонный референс. **✅ ВЕРИФИКАЦИЯ АКУСТИКОЙ (после компакта, сессия 19-прод):** эмиссии rec7
  пере-сохранены (`work/rec7_w2v_emis.npy` свежий, 12718×51, stride 20.02мс); декод зон подтвердил все
  8 мест. `check_live_rec7.py` даёт «6/8» лишь по грубой эвристике «rep-слово в ≤2 словах» — м11 ذلكم
  и м13 وكيل чинятся не собственным возвратом, а верной раскладкой после возврата соседа: ذلكم декод
  213–215с=«ذلكم الله ربكم» ✓; وكيل декод 232–233с ✓, span 2.3с (был 6.4с) — БОЛЬШЕ НЕ проглатывает
  لا تدركه (та читается 234–238с + перечитка @238.6/243.1 = «×2» владельца). 9 возвратов = реальные
  перечитки в декоде, ложных петель НЕТ, fj=0. ⚠️ ЕДИНИЦЫ: `w2v_align.emissions()` даёт stride в МС —
  при декоде делить t(с) на stride/1000. **🔵 СЛЕДУЮЩЕЕ:** ждать слух владельца по rec7 (8/8 — реально
  ли лучше?). Инструменты: `work/decode_rec7_zones.py`, `work/check_live_rec7.py`, `work/regen_emis_rec7.py`. ⚠️ Грабли: ISOLATE gpu_align оставляет ОРФАН-процесс после
  run_one (VRAM=15МБ, но чистить `kill` в контейнере перед новым прогоном, иначе арена→OOM).

- **Сессия 18-продолжение (25.07, tg_3795/3805/3881): владелец ОТМЕНИЛ ожидание эталона — «чини
  перечитки rec7 САМ по акустике, не жди мой ручной ввод, может его и не будет».** Сделано (main):
  (1) **CTC-likelihood детектор перечиток** (`w2v_repeats._ctc_fwd_group`, коммит `da935ab`) — вместо
  шумного greedy-декода в текст CTC-скор гипотез MADD vs CONT прямо по эмиссиям (Viterbi best-path
  per-frame); аддитивно к greedy (вынес в `_greedy_group`, CTC — fallback когда greedy пусто → не
  трогает существующие возвраты, rec13 цел). Гарды от замазки: маржа + ВТОРОЕ ЧТЕНИЕ фразы после span'а
  (иначе под-сегментация одного прохода, не перечитка) + мин.кадров/слово + пост-гард fj≤base.
  **rec7 GPU-перегон: reps 8→10, fj=0, cov 0.927, ново починено МЕСТО 13** (وكيل span 6.4с проглотил
  1-е чтение لا تدركه الأبصار → подсветка وكيل→لا تدركه×2); خضرا(м1)/صاحبة(м8) целы. (2) **Полное
  описание алгоритма** → `docs/ALGO-current-align.md` (ответ на tg_3881 про паузы: w2v НЕ выдаёт «конец
  слова/паузу», только буквы+blank; паузы используем вторично — снап границ + якорь перечитки; первичный
  якорь пауза быть не может — в таджвиде нет пауз между словами, только на фразах/вакфах). (3) **manual
  save тащит rep** (`03ef284`) + **compare_manual контекстный отчёт** (`f0645c9`, окружение визитов +
  ОБГОН/ЗАЛИПАНИЕ подсветки на пропущенном возврате). **УПЁРЛОСЬ (места 6/10/11/14 — السماوات/عليم/
  ذلكم/وهو):** CTC-BACK тест (`work/test_ctc_back.py`) — madd фитит ЛУЧШЕ любой обратной фразы (Δ<0),
  т.е. это генуинный долгий мадд, перечитка СЛИТА монотонным Viterbi → без слуха владельца не отделить
  от допустимого распева (предел, как у 4 отвергнутых абс.метрик). Фабрикация = замазка, НЕ делаю.
  **🔵 СЛЕДУЮЩЕЕ:** ждать слух владельца по rec7 (места 1/8/13 — стало лучше?); если ок — перегнать
  остальные 8 рек новым детектором (пока НЕ трогал: гард 2-го чтения меняет reps rec9 29→24, rec5 14→12
  — не менять реки, что он считает ОК, вслепую). Места 6/10/11/14 — ручной эталон ИЛИ межсловный детект
  2-го вхождения. Инструменты: `work/proto_ctc_ratio.py`, `val_repeats_all.py`, `test_ctc_back.py`.
  ⚠️ Грабли поллера: при резюме после компакта развелось 6 Monitor-поллеров (спам ×6) — схлопнул
  TaskStop до одного (bbzujmktq). Проверять TaskList/дубли notification при резюме.

- **Сессия 16 (25.07): свежая ЖЁСТКАЯ указка владельца (tg_1786..1958) + честная диагностика rec7.**
  Владелец (голосом, много сообщений, зол на переусложнённость): (1) **выкинуть деление «аллайнер vs
  распознаватель»** — оба выдают буквы/слова, матчинг с Кораном = ОДНА универсальная функция/модуль;
  (2) **каждый распознаватель полностью независим**, плоский массив (google/whisper/w2v), ноль
  наследования; (3) **один файл = один распознаватель**, динамический импорт — дал ТОЧНЫЙ референс
  (tg_1958): `~/work/wildbox/airflow-backend-analytics/dags/analytics_notifications` (subscriptions/
  файл-на-тип, `importlib.import_module(f'.subscriptions.{field}')`, единый контракт get_to_notify/
  format_to_text). Референс изучен, паттерн подтверждён. **Сделано в сессии 16:** обновил
  `docs/SPEC-alignment.md` (w2v-реальность сессии 15: свой w2v_repeats, кейс D — inherit убран, кейс F
  rec10 ЗАКРЫТ; **кейс E — активная жалоба владельца на rec7-хвост**) и `docs/ARCH-independence-refactor.md`
  (референс подтверждён, целевой пакет `sources/` + контракт `run()`, статус 5-шагового плана,
  безопасный порядок 5-DSP→2-match_align→4-плагины→3-MMS-range). **Диагностика rec7 хвоста (offline,
  `work/rec7_w2v_emis.npy` + `work/probe_rec7_*.py`, `work/test_trim_ba.py`):** FWD-возврат وخلق كل شيء
  ×2 ЛОВИТСЯ верно (улучшение сессии 15); гипотезу «FWD захватывает лишний وهو» проверил before/after
  на всех 9 реках — **no-op, ОТКЛОНЕНА** (акустика оправдывает границы), трим откачен, замазку не делаю;
  реальный остаток — **дефект №2: дрейф ±1-2с на разрежённом распеве** (ось B, предел акустики; зона أنى
  почти пустой декод) → нужен softmax-mass сигнал, не greedy; отдельным заходом. Всё в main.
  **🔵 СЛЕДУЮЩЕЕ (Task #2, активно): рефакторинг в плоские плагины.** ✅ ИНКРЕМЕНТ 1 СДЕЛАН
  (сессия 17): w2v→falign РАЗВЯЗАНА — 5 самодостаточных кирпичей (`_HARAKAT`/`_frame_db`/`_snap_bounds`/
  `_sim`+`_lev`/`_collapse_tandem` + `SAMPLE_RATE`/`_SNAP_*`) вынесены в `src/alignbricks.py`; falign
  реэкспортит под старыми именами (gpu_align/pipeline/tasks не тронуты), w2v_align/w2v_repeats импортят
  из alignbricks (больше НЕ `import falign`). Поведение-сохраняюще: reps 11/9/7/21/4/20/14/12/8
  идентичны, `manage.py check` чист. ✅ ИНКРЕМЕНТ 2 СДЕЛАН (сессия 17): создан `src/match_align.py` —
  ЕДИНЫЙ модуль матчинга к Корану, два входа: СЛОВЕСНЫЙ (`align`/`load_transcript`/`match_stats`/
  `CorpusIndex`/`Word` из align.py) + БУКВЕННЫЙ (`find_range`/`build_index`/`greedy_skeleton`/… из
  w2v_range.py). `align.py` и `w2v_range.py` теперь тонкие ре-экспортные шимы (pipeline/run/gpu_align/
  пробы не тронуты). Поведение-сохраняюще: OLD==NEW байт-в-байт на rec7-эмиссиях, словесный align rec11
  cov 0.964, `find_range` с сохранёнными декодами 9/9 (rec7 6:95-103), `manage.py check` чист. Коммит в
  main. Инструмент: `work/verify_match_align.py`. ✅ ИНКРЕМЕНТ 4 СДЕЛАН (сессия 17, коммит `93cbddf`):
  **плоские плагины-источники.** Создан пакет `service/recitations/sources/` (файл-на-источник + лоадер
  `__init__.py` через `pkgutil.iter_modules`+`importlib`, реестр по PRIORITY). 5 источников
  manual(P5)/w2v(P10)/forced(P20)/google(P30)/whisper(P40), контракт `KEY/LABEL/NOTE/SELECTABLE/AUTO/
  ISOLATE/ALIGNED/PRIORITY` + `available()/ready()/run(rec,audio,quran,out,stage)`. **Удалено:**
  `recognizers.py` целиком (REGISTRY/ALIGNERS/is_aligner/PRIORITY/selectable), `pipeline._recognize`+
  `_inherit_repeats`+`_REPEAT_ZONE_MARGIN`, `tasks._maybe_forced`/`_maybe_w2v` (→ обобщённый
  `_ensure_auto` по `sources.auto()`), ветвление `run_one`/`gpu_align` по типу. Единый диспетчер:
  `run_one`→`sources.get(k).run()` (ISOLATE→подпроцесс gpu_align, иначе in-proc), `gpu_align` грузит
  модуль по ключу. `active_run`→`sources.keys()`/`is_aligned` (логика та же). views/models на `sources`.
  Поведение-сохраняюще e2e rec5: google/w2v/forced word_timeline байт-в-байт идентичны (cov 0.977/0.914/
  0.478, fj0), `run_single`→`_ensure_auto` гонит w2v+forced, VRAM свободна, 0 осиротевших gpu_align,
  `manage.py check` чист. ✅ ИНКРЕМЕНТ 3 СДЕЛАН (сессия 17, коммит `3298a69`): **forced ПОЛНОСТЬЮ
  независим — свой диапазон из акустики MMS.** `_forced_source`+гейт `ready()` сняты. `match_align`:
  `find_range` сделана алфавит-агностичной (общее ядро `_index_over(quran, tok_str, k)`); добавлен
  `build_romanized_index` — двойник индекса в РОМАНИЗОВАННЫХ согласных скелетах (uroman+unidecode = тот
  же алфавит, что MMS-greedy-декод). Романизованный путь `k=_K_ROM=7` (~20 латинских согласных → k=5
  коллизии, пик плотности уезжал); шаг (е) обобщён в **мульти-сид по top-`_SEED_SURAS=8` сурам плотности
  с выбором по реальному difflib-ratio** — робастность к ложному пику (истиаза-интро матчит 16:98;
  рефрен Ар-Рахмана размазывает суру). `falign.align()` расщеплён на `emissions()`/`align_verses()`/
  `whole_decode_skeleton()` — один MMS-проход и на локализацию, и на выравнивание (align() — тонкая
  обёртка, поведение цело). `sources/forced.py`: run() = свои эмиссии→find_range(романиз.)→align_verses;
  AUTO без предусловия. `_forced_source` удалён; forced_align-команда `--all-ready`→`--all`. **Валидация:**
  w2v-путь (арабский k=5) 8/9 без регресса (`work/verify_match_align.py`; rec7 — известный артефакт
  устаревших эмиссий, live верно); forced-самолокализация e2e **7/9 точно** (id5/9/10/12/13/14; Ар-Рахман
  id10 55:1-78 ПОЧИНЕН мульти-сидом), id6/id7 off-by-one на краю (зачин كٓهيعٓصٓ / хвостовой аят — предел
  акустики MMS, w2v-дефолт их берёт верно). Офлайн-фикстуры `work/rec{5,10,14}_mms_emis.npy`.
  ✅ ИНКРЕМЕНТ 5 (чистка) СДЕЛАН (сессия 17, коммит `3172fb2`): **Task #2 ЗАКРЫТ ЦЕЛИКОМ.** Связь
  `w2v_*`→`falign` была разорвана ещё в инкременте 1 — `w2v_align`/`w2v_repeats` импортят `alignbricks`
  напрямую, `w2v_range`→`match_align`, `match_align` `falign` НЕ касается (транзитивно проверено:
  `import w2v_range,w2v_repeats` без falign в sys.modules). Оставался переходный хвост — falign
  реэкспортил `_lev`+часть `_SNAP_*` «на время перехода», но их не звал НИКТО (ни falign внутри, ни
  снаружи через `falign.*`) → снял из импорта, falign тянет из alignbricks только реально используемое.
  Единственные легитимные `import falign` — `sources/forced.py` (потребитель) + `test_falign.py` (тест
  falign). Декод-стратегии (`_greedy_ctc`/`_skeleton` у falign, CTC-Viterbi у w2v, `greedy_skeleton`/
  `_rom_skeleton_word` у match_align) СПЕЦИФИЧНЫ для источника — НЕ обобщаю (обобщить = вернуть связь,
  против директивы «ноль наследования»). Поведение-сохраняюще: verify 8/9 (rec7 — известный артефакт),
  forced rec5 e2e 12с/156сл/cov 0.478 идентично, `manage.py check` чист, VRAM свободна, 0 сирот.
  **rec7 дефект №2 (кейс E, дрейф на распеве) — исследовано (сессия 17, коммит `4ffc0a0`):**
  автопроверка оси B на wav2vec2-побуквенной-активности (`loud_gaps`/`frozen_sec`, 4-й кандидат спеки)
  ПРОТОТИПИРОВАНА на всех 9 и ❌ ОТКЛОНЕНА — держания rec7 (дрейф) статистически идентичны здоровым
  rec5/6 (act% медиана 0.075 vs 0.079/0.077, rec7 даже ниже; длинный разрежённый мадд на рифме =
  норма стиля). softmax-масса как ПОБУКВЕННЫЙ абсолютный сигнал онсета тоже опровергнута (та же
  причина). Итог: абсолютной пороговой метрики оси B НЕТ (ни MMS, ни RMS, ни wav2vec2) — 4 подхода
  отвергнуты, не изобретать 5-й порог. Уцелел ОТНОСИТЕЛЬНЫЙ путь (сверка со 2-м независимым декодом:
  forced-онсеты в здоровой зоне / MFCC-самоподобие) — метрика, не наследование; дорого, отдельным
  заходом. Инструмент: `work/proto_acoustic_health.py`+`proto_ah_sep.py` (офлайн по `rec*_w2v_emis.npy`).
  **✅ П12-переделка СДЕЛАНА (сессия 17, коммит `655adde`, tg_2544):** жалоба владельца «текст
  сплошняком → мышь при переводе с конца строки задевает пол-суры → каша» закрыта тремя правками в
  `manual.html` (чисто клиент): (1) **DWELL-фильтр** — интервал пишется только если курсор отдохнул
  на слове ≥130мс; задевания при переводе руки не набирают dwell → отбрасываются (уход закрывает
  точно, приход взводит таймер, старт бэкдейтится, перемотка пере-взводит якорь `reArmHover`);
  (2) **регулятор скорости** (`[`/`]`/select + localStorage) — замедляет чтеца, времена в аудио-шкале
  от playbackRate независимы; (3) **автоскролл-телепромптер** — маркер `.at` по исходному прогону
  (`posRef`), текст едет за ним (полоса 30–55%, цель 38%), скроллит ТОЛЬКО в паузах записи (не дёргает
  из-под курсора), ручной скролл уважается 2.5с, тумблер+LS. Playwright: задевание 8 слов@20мс→**0
  интервалов**, задержка 320мс→1; скорость select/`[`/`]`; маркер+автоскролл 0→667→1054 в полосе 0.38,
  OFF→замер. Остаток v2 (ждёт руки владельца): диапазон аятов с нуля, видео-источник, тюнинг DWELL_MS.
  **✅ Ось B — ОТНОСИТЕЛЬНЫЙ детектор дрейфа ПРОТОТИПИРОВАН (сессия 18, коммит `c6db88e`):** 5-й
  подход после 4 отвергнутых абсолютных. Для долгого держателя декодим его СОБСТВЕННЫЙ span и сверяем
  `held`(=skel[i]×k, мадд) vs `cont`(=skel[i..j], held+продолжение); `cont≫held` = span накрыл
  следующие слова = перерастяжение таймлайна. **В ОТЛИЧИЕ от 4 абсолютных (плоские, rec7==здоровый) —
  сигнал НЕ плоский:** декод собственного span'а буквально содержит слова продолжения (rec11 53:31
  عملوا cont=**1.0**→عملي يجزي الذين حسن; rec9 17:47 بِهِ cont=0.75/held=**0.0**). НО (а) rec7-кейс
  владельца (6:101 صاحبة) НЕ флагуется — это FWD-перечитка وخلق كل شيء (ловится `w2v_repeats`), не
  плоский дрейф; (б) срабатывает ШИРОКО (rec6=2/rec9=8/rec11=3/rec12=2/rec13=2/rec14=1) вкл. «здоровые»
  → без слуха владельца не отделить «слышное залипание» от «допустимого мадда»; авто-трим НЕЛЬЗЯ
  (где резать — пословный ре-алайн, ошибка = forward-jump = замазка). Итог: относительный путь = 
  ранжированный список кандидатов-перерастяжений (топ cont≥0.8&held≤0.4: rec11 53:31, rec12 56:9,
  rec13 56:94, rec9 17:49/17:47), калибровка = слух владельца (как reps). Инструмент:
  `work/proto_drift_relative.py <rec>`; детали — `docs/SPEC-alignment.md` §3.2 (5-й подход) + BACKLOG WE.
  **✅ СЕССИЯ 18 — СЛУХОВОЙ ВЕРДИКТ ВЛАДЕЛЬЦА по rec7 + 4 деливербла (карт-бланш по токенам до 16:00,
  self-compact окна 30мин/контекст до 300-400к).** Владелец голосом прошёл rec7 6:99-6:103, дал 14 мест
  (лог `work/owner_ear_verdicts_s18.md`). Классы дефектов: (A) BACK-перелёт влево на 1 слово; (B) FWD-
  ПЕРЕЧИТКА ФРАЗЫ не отыграна — подсветка залипает на последнем слове, синхрон со 2-го прохода (самый
  частый, 7 мест); (C) подсветка ОБГОНЯЕТ голос вперёд на повторе (вкл. главный: хвост 6:101 وخلق كل شيء
  عليم → подсветка убегает на ذلكم 6:102 на ЦЕЛЫЙ аят); (D) визуал (заливка/пропуск عليم). **Сделано
  (всё в main):** (1) убрал бегущую karaoke-заливку → сплошное слово (`b1882f8`, tg_3604: «моргающий
  светофор»); буква+харакат ОТЛОЖЕНО (владелец боится порчи вязи спанами — char_timeline у нас ЕСТЬ,
  вернуться после ручного, иначе маркер-оверлей). (2) **Место 1 (класс A) ПОЧИНЕНО** (`4d176f9`):
  BACK-перелёт خضرا — sim ra=شيء и ra=فأخرجنا РАВНЫ, старый `>` брал перелётный; фикс `_BACK_TIE_EPS`
  тай-брейк → меньший откат; перегон rec7 w2v GPU, REP=[99:11..13] فأخرجنا منه خضرا. (3) **Ручной элайнер
  v3** (`6901b92`, tg_3610): hover/dwell ВЫКИНУТ, только КЛИКИ+КЛАВИШИ — →/← фокус, клик=возврат
  (перечитка→rep), ↑/↓ скорость (до1×/0.1×), Space пауза; пишешь с нуля по слуху → ЭТАЛОН. (4) **Валидатор
  `tools/compare_manual.py`** (`7bf24d3`): сверка прогона с ручным эталоном по возвратам/порядку
  (difflib ratio + precision/recall возвратов), мс не в счёт — как просил владелец. **🔵 СЛЕДУЮЩЕЕ:**
  классы B/C (11 из 14 мест) детектор `w2v_repeats` ПРОПУСКАЕТ (ловит только длинно-span'овые خضرا/صاحبة)
  → владелец делает ручной ЭТАЛОН rec7 по слуху → тюнинг детектора против него (`compare_manual`).
  Поднимать «улов» вслепую НЕЛЬЗЯ (ложняки-каша на здоровых). Ждать его эталон/фидбек по ручному элайнеру
  ИЛИ взять верхний невыполненный из BACKLOG. reps-точность + дрейф-кандидаты ждут СЛУХА.
  **Ниже — ось B (сессия 18 ранняя):**
- **Сделано 24-25.07 (сессия 15): w2v ПОЛНОСТЬЮ независим — whisperx выкинут, свой CTC-Viterbi +
  точный find_range.** Директива владельца: «Wave2Vec отдельный, зачем ему WhisperX?» + «ничего от
  других ASR/аллайнеров». Сделано: (1) модель грузим напрямую через `transformers`
  (Wav2Vec2ForCTC/Processor), аудио — `soundfile` (`w2v_align._load_wav`), эмиссии считаем сами
  (log-softmax логитов) — один GPU-проход; (2) **свой монотонный CTC-Viterbi** (`w2v_align.forced_align`
  / `_ctc_viterbi`): расширенная посл-ть [blank,l0,blank,...], stay/+1/+2, backpointer'ы → кадр на
  метку; скип на blank запрещён → каждой метке ≥1 кадр → НЕ схлопывается (**collapsed_words=0 везде**,
  было 14 на rec7 из-за whisperx-рутины). Слово владеет временем до онсета следующего (мадд честно
  висит), снап к тишине триммит паузы. Окна больше не нужны (путь глобален). (3) **find_range точность**
  (пересчёт вскрыл ошибки): IDF-взвешивание k-грамм (убрало пик от истиазы/басмалы → правильная СУРА
  у всех) + регион ≥ вся сура пика (короткоаятные суры влезают) + добор границ (приближение по
  покрытию через префикс-суммы + _difflib_score + внутрисурный добор + срез одиночных хвостов +
  2-й сид «целая сура пика» по реальному ratio для повторяющихся сур — см. rec10 ниже). **9/9
  диапазонов ТОЧНО** (rec5/6/7/9/10/11/12/13/14). find_range: Марьям 14мин→12с (был O(регион²)). Коммиты
  в main. Инструменты: `work/decodes.json` (сохранённые декоды), `work/exp_findrange.py` (офлайн-подбор
  границ без GPU). memory [[w2v-independent-viterbi]].
  **✅ ЗАКРЫТО 25.07 (сессия 15, Task #3): ВОЗВРАТЫ (П8) на СВОЕЙ акустике w2v.** Причина reps=0:
  монотонный Viterbi НЕ оставляет дыр → gap-детектор молчал. Возврат в w2v = span слова-остановки
  РАСТЯГИВАЕТСЯ и поглощает перечитку/распев. Новый триггер — аномально длинный span (≥2.5×медианы),
  внутри акустический разбор greedy-CTC декода span vs скелеты: FWD (мелодичный обгон впередней фразы,
  [i+1..j]) / BACK (классический возврат назад [ra..i]). Дискриминаторы против шумного распева: декод
  многословный (≥2×скелет держателя), победитель обгоняет held-нуль (держатель-мадд) и другое
  направление, сбалансированный difflib-ratio (не одностороннее покрытие), пропуск интро (басмала)/
  односимвольных держателей. `src/w2v_repeats.detect` переписан (gap→span). rec7 (кейс владельца):
  خضرا 6:99 назад (نخرج منه حبا متراكبا) + صاحبة 6:101 вперёд (صاحبة→وخلق كل شيء распев, потом чёткая
  перечитка ×2), forward_jumps=0, collapsed=0, cov 0.795→0.927. build_data (player.py:79) тащит флаг
  `rep`. _inherit_repeats выкинут (нарушал независимость). Тюнинг офлайн БЕЗ GPU: `work/tune_repeats.py`
  + `work/rec*_w2v_emis.npy` (эмиссии в файлах). Коммит в main. **Владелец должен послушать rec7.**
  **✅ rec10 Ар-Рахман ЗАКРЫТ 25.07 (сессия 15):** find_range чинён вторым сидом (RAW целая сура
  пика) с выбором окна по МАКСИМАЛЬНОМУ реальному difflib-ratio. Корень: рефрен «فبأي آلاء ربكما
  تكذبان» ×31 путал ОДНО глобальное difflib-выравнивание → cov по суре 55 ≈0 → префикс-макс уводил
  в соседа (55:77..56:39). Истина 55:1-78 имеет ratio 0.32 = максимум → второй сид её ловит;
  частичные чтения не задеты (у целой суры ratio ниже). Офлайн 9/9 диапазонов (было 8/9), e2e GPU
  rec10: range=55:1-78 cov=0.998 fj=0 **reps=4** (возвраты включились — до фикса reps=0 из-за
  неверного диапазона). `src/w2v_range.find_range` шаг (е). Офлайн-стенд `work/exp_findrange.py`.
  **🟡 Мануальный элайнер (П12) — переделка (владелец tg_2544):** текст сплошняком → мышь при переводе
  с конца строки задевает пол-суры → каша. Нужно: скролл текста, сниж. скорости, фильтр случайных
  задеваний/проходов. Отдельная тема.
- **Сделано 18.07 (сессия 14): w2v в docker на GPU (WD закрыт) + GPU-изоляция подпроцессом.**
  wav2vec2/whisperx теперь в docker-воркере (образ), w2v — авто-пост-шаг (`tasks._maybe_w2v`, зеркало
  `_maybe_forced`) и ДЕФОЛТ в плеере. **CUDA-стек унифицирован на cu124:** torch==2.5.1+cu124 приносит
  cuBLAS 12.4.5.8 + cuDNN 9.1.0.70 (как раньше) + прочее 12.4.127 — один набор на torch(w2v) +
  onnxruntime-gpu 1.20.1 (MMS forced, CUDA EP активен) + ct2 (whisper); ручные nvidia-* пины (частью
  12.9) убраны из requirements (конфликтовали). whisperx ставим `--no-deps` + torch/torchaudio/
  transformers==4.46.3/pandas/nltk (хвост faster-whisper/pyannote/onnxruntime-CPU не нужен, конфликтует
  с onnxruntime-gpu). `PYTHONNOUSERSITE=1` (в HOME=/app/.cache mount валялся user-site transformers 5.x,
  ломал whisperx). **ГЛАВНОЕ — каждый GPU-выравниватель отдельным процессом** (`recitations/gpu_align.py`
  ← `pipeline._run_aligner_subprocess`): onnxruntime держит ЛИПКУЮ CUDA-арену → forced+w2v в одном
  процессе = OOM на 6ГБ; подпроцесс вышел → VRAM свободна. e2e rec5: forced cov=0.478 + w2v cov=0.823
  (156/156 t_end) оба ready, дефолт=w2v. Коммит `477ed06` в main. memory [[w2v-docker-subprocess]].
- **Сделано 18.07 (сессия 14): WR — возвраты чтеца (П8) на w2v (коммит `20c393d`).** Второй
  GPU-проход MMS НЕ понадобился: детектор `falign._detect_repeats` уже отработал в forced-прогоне
  (эмиссии посчитаны, rep-точки в forced/sync-map.json) → ПЕРЕНОС результата на w2v.
  `pipeline._inherit_repeats(rec, sync_map)` в `run_one` при `recognizer==W2V` (до build_data).
  **Инсайт — простой перенос rep-точек = ЗИГЗАГ:** монотонный w2v на записи с возвратом ломает
  раскладку ВСЕЙ зоны повтора (rec7: wi14-17 затолканы в окно перечитки 88-91с, wi17 растянут на
  6с), forced же выразил её чисто (дыра→перечитка wi11,wi12→wi14+ после 91.5с). Поэтому замещаем
  всю ЗОНУ ВОЗВРАТА forced-точками (там forced корректен), остальное аята — w2v (честный coverage).
  Зона = непрерывный диапазон wi с расхождением forced↔w2v по времени > `_REPEAT_ZONE_MARGIN`=1.0с
  ∪ rep-слова. rep-источник = forced/sync-map.json (поле `rep` роняет build_data src/player.py:79).
  e2e: rec7 خضرا wi13→wi11(←)→wi12→wi14+ монотонно (cov 0.706→0.604); rec11 кросс-аятн rep_inh=11
  cov=0.837; rec12 rep_inh=2; rec5 чистая rep_inh=None; дефолт=w2v везде. memory [[w2v-repeats-inherit]].
  **🔵 СЛЕДУЮЩЕЕ — владелец хотел САМ посмотреть фиксы (WQ вакфы + WD docker + WR возвраты),
  ПОТОМ выгрузка github.io.** Дальше автономно: обновить выгрузку github.io (DEPLOY.md §6,
  [[ghio-deploy]]) свежими w2v-дорожками с возвратами — либо ждать ЕГО вердикт (нужен его слух/телефон).
- **Сделано 13.07 (сессия 13): align теперь протаскивает реальный t_end распознавателя + whisper
  переиспользует кеш raw.json.** Владелец давно указывал «align херовый — оперирует только началами
  слов». Оказалось: whisper/google STT дают конец слова (`a[i].end`), якорные `points` несли `t_end`,
  но `align._segment.push_word` клал в `word_timeline` только `t` (плотная дорожка интерполирует
  времена между якорями → у синтетических слов реального конца нет, поэтому весь трек унифицировали
  на `t`). Фикс: `push_word(t, corpus, t_end=None)` несёт `t_end` от ЯКОРЕЙ (реальный конец ASR),
  интерполированным не даёт. Плеер УЖЕ умел t_end (`player.html:542` — заливка замирает на паузе, не
  тянется через тишину), build_data УЖЕ протаскивал (`player.py:77`) — не хватало только звена в align.
  Эффект на coverage (метрика `_speech_time_coverage` считала CAP=0.6с вместо реальной длины слова →
  занижала в 2-3.5×): по всем 9 записям google/whisper cov 0.35-0.5 → **0.85-0.97** (rec10 хвост
  ниже: google 0.467 — реально пропускает куски). Попутно: whisper всегда жёг GPU заново, игнорируя
  сохранённый raw.json (google переиспользовал) → теперь симметрично, переalign из кеша 0.3с против
  ~30с GPU. **Побочка:** новые cov сменили выбор `_forced_source` на rec7/11 (google→whisper, диапазон
  тот же, forced идентичен) и rec13 (whisper→google, диапазон 56:2→**56:1**, +7 слов). Все google/whisper
  переалайнены из кеша, forced перегнан где сменился источник; на нетронутых forced word_timeline
  идентичен (проверено rec5 семантически — изменилось только поле `runs`/метрики). Коммиты в main:
  align t_end, whisper cache. **Остаток идеи (в беклог):** интерполированным словам конца не даём —
  можно точнее (char-level или held-гласная), но для прототипа хватает.
- **✅ ЗАКРЫТО 13.07 (сессия 12): ИСХОДНЫЙ таск github.io — плеер выложен на GitHub Pages.**
  Живёт на `https://risabd.github.io/syncronized/` (сабмодуль `syncronized` → этот репо, ветка
  `github.io`; репо публичный → Pages клонирует по https). **Актуальная выгрузка (13.07, сессия 13):
  9 записей** (5,6,7,9,10,11,12,13 + новая 14); коммит github.io `c86be30`, сабмодуль в
  risabd.github.io `166a4a4`; все точки на живом url 200 (recitations.json=9 recs, r/14/data.json,
  rec13 forced теперь с 56:1). **Владелец
  (13.07) поправил подход: НЕ автоматизировать выгрузку** («на каждый чих не надо, вручную запускать
  только») → `tools/build_ghio.py` теперь гоняется ВНУТРИ docker-воркера (`docker compose exec -T worker
  python manage.py shell < tools/build_ghio.py`, RequestFactory=паритет с бэком, веб на 8000 НЕ нужен —
  порт занят чужим), пишет `./work/ghio-export` (forced по умолчанию, manual-прогоны выкинуты, `audio=""`).
  Дальше руками: worktree ветки github.io → rsync выгрузки → commit/push; в `../risabd.github.io`
  `git submodule update --remote syncronized` → commit/push. **Полный рецепт — `docs/DEPLOY.md §6`.**
  remote-control поллер (pid при спавне) — ПЕРЕЗАПУСКАТЬ после компакта (single-poller guard не даст дубль).
- **🔴 12.07 (сессия 12): АВАРИЯ + полное восстановление.** При попытке создать ветку `github.io`
  через git-worktree команда `git worktree add … main` упала (main занят основным worktree), `cd`
  не сработал → последующий `find . -maxdepth 1 -exec rm -rf` снёс ОСНОВНОЕ рабочее дерево.
  Отслеживаемое вернул из git; **безвозвратно потеряно gitignored:** `db.sqlite3`, `cache/` (модели),
  `creds/`, `.env`. Восстановлено: (1) creds из `../speech-to-text-python/pacific-vault-*.json` →
  `creds/gstt.json`; (2) `.env` = `SYNC_GSTT_BUCKET=pacific-vault-142810.appspot.com`; (3) whisper
  tarteel переконвертил в ct2 (`cache/ct2-tarteel-base`, + модель в `~/.cache/huggingface`); (4) forced
  MMS-onnx перекачал (была битой); (5) БД пересоздал, 6 YouTube-рек ре-импортировал ЧЕРЕЗ БЭК по ссылке
  (google live + whisper + forced, все ready); (6) вернул прежние id 5/6/7/9/10/11 (`tools/remap_ids.py`
  + переименовал `media/rec/`). **Рецепт развёртки с нуля → `docs/DEPLOY.md` (ВЕДИ его).** Попутный фикс
  бага: `_maybe_forced` брал ключ из `set` ALIGNERS (порядок зависит от хеш-сида) → писал forced как
  «manual»; теперь `rz.FORCED`. **Грабли, что стоили времени:** docker пересоздаёт отсутствующие
  bind-папки как root → `work/`/`cache/` не писались (chown из контейнера `-u 0`); yt-dlp — дефолт-клиент
  (принудительные android/ios YouTube сломал). **Урок: НИКОГДА не `rm -rf` в основном дереве; worktree
  создавать на НОВОЙ ветке (`git worktree add -b <new> <path>`), не на занятой main.**
- **Идея в беклог (вопрос владельца 12.07): concurrency в celery через ОЧЕРЕДИ, не код.** Чтобы google
  (сеть) шёл параллельно, а GPU-шаги (whisper/forced) по одному — раздельные очереди (`task_routes`
  gpu/default) + два воркера `-Q gpu --concurrency 1` / `-Q default --concurrency N`. «По одному» =
  свойство ДЕПЛОЯ (сколько gpu-воркеров), не зашитый лок (я по ошибке зашивал filelock — откатил,
  владелец поправил: это свойство его 6ГБ-карты, не кода). Дефолт compose снова `--concurrency 1`.
- **Сделано 11.07 (сессия 11): П12 v2 — сохранение ручной привязки как прогон «manual» в бэк.**
  Ручной элайнер замкнут: правишь мышью → «💾 Сохранить в бэк» → `POST /r/<id>/manual/save`
  (word_timeline urlencoded, без CORS-preflight) → `pipeline.build_manual_run` собирает sync_map
  (аятные якоря + слова) → ТОТ ЖЕ `build_data`, что forced/ASR → прогон «manual» выбираемый в плеере
  наравне с остальными (с sections+Diyanet+метриками). `recognizers`: «manual» первым в REGISTRY
  (высший приоритет авто-выбора, человек=истина) + в ALIGNERS (исключён из selectable и `_forced_source`).
  Синхронно (без нейросети), зовётся из вьюхи. Проверено end-to-end (web, forced-wt rec11 386 сл →
  manual ready+активен, тестовый прогон удалён, дефолт откачен на forced) + playwright 15/15 (+3 на
  кнопку), node --check + `manage.py check` web/worker чисто. **Остаток v2 (ждёт фидбек):** диапазон
  аятов с нуля (без ASR); видео-источник; клавиши-настройки. Детали — BACKLOG таблица П12.
- **Записано правило (голос владельца 11.07): бюджет-гард на пробуждении после `/self-compact`** —
  ПЕРВЫМ делом прогнать `usage-check`, глянуть 5-часовое окно; если остаток <5% (использовано >95%) —
  НЕ возобновлять работу, остановиться (оставить запас). **Гард — правило ЭТОГО проекта** (живёт
  здесь + в memory [[self-compact-cadence]]); глобальный скилл self-compact про бюджет не знает,
  проверку делаем сами на пробуждении. (NB: 11.07 владелец разово попросил игнорить <5% и «жечь лимиты
  до 16:00» — недельный сброс; это разовое, правило-гард по умолчанию действует.)
- **Сделано 07.07 (голос владельца, приоритетная указка `new_tasks/`): П10 — плеер, выбор
  размера шрифта + сплошной поток аятов.** Три правки в `player.html`: (1) выбор РАЗМЕРА (не
  только шрифта) — `--ayah-size`, панель «⚙ Шрифт» → секция «Размер шрифта» (`A−`/ползунок/`A+`,
  localStorage `sync.arabsize`); (2) аяты — инлайн-`<span>` вместо блоков → один сплошной
  justify-поток как мусхаф; (3) снято увеличение текущего аята (`.ayah.active` без `font-size`)
  → нет reflow → скролл непрерывный без «резких прыжков» (это и была их причина). Фокус теперь
  только цветом + словное окно. Проверено playwright: активн.слово==соседнее==#text (35.2px),
  `inline`, `justify`; ползунок пишет в localStorage. ТЗ-аудио+транскрипт → `media/` (конвенция),
  `new_tasks/` удалён. Детали — BACKLOG «Сделано» + таблица П10.
- **Сделано 10.07 (сессия 8) — П8 «возврат чтеца назад» РЕАЛИЗОВАН (v1, акустика+паузы) + перегон
  forced на GPU закрыт.** `src/falign.py::_detect_repeats` (детали — BACKLOG п.6, NOTES верх).
  Дыра в монотонном таймлайне + речь (RMS) + greedy-CTC-декод эмиссий MMS → согласный скелет →
  матч на «назадние» слова эталона (Левенштейн ≥0.5, лучше «вперёд») → вклейка назадних точек в
  `word_timeline` (фронт не менян, `bsearch` по `t` → `wi`). Проверено rec7 خضرا (6:99): подсветка
  идёт назад wi13→wi11→wi12→вперёд wi14. rec1/5/6/9/10 — 0 ложняков (чистые). Опт-аут
  `SYNC_FALIGN_REPEATS=0`. Все 6 forced перегнаны через сервис на GPU (`ready`, CUDA, cov
  0.977–1.0, snap везде). **Тюнинг порога `_REPEAT_MIN_SIM` — по фидбеку с новых ссылок.**
- **Сделано 10.07 (сессия 8, продолжение):**
  - ✅ **Параллельные ASR** (указка `task.md`): google (облако) || whisper (GPU) в потоках
    (`tasks._run_parallel`), forced после; SQLite→WAL+busy_timeout. Между записями concurrency 1
    (GPU-семафор → BACKLOG «Позже»).
  - ✅ **Фидбек `task.md` п.2 (خضرا обделён фокусом):** якорь возврата на КОНЕЦ ПАУЗЫ в разрыве
    (не с g0) → слово-остановка держится сквозь паузу; плеер уважает `t_end` (build_data пробрасывает)
    → заливка на паузе замирает на 100%. rec7 خضرا: держится 85.86→88.46, прыжок на 1:28.5.
- **Сделано 11.07 (сессия 9, указка `new_tasks/task2.md`): task2 идея (1) РЕАЛИЗОВАНА — растяжка
  stuffed-слов вместо отсева.** Долгий مadd ломал forced (rec11 Ан-Наджм 53:3→4): الْهَوَىٰ 0.24с,
  53:4 скомкан и выброшен → 6.4с дыра → залипание. Фикс: `falign._stuffed_runs` + `_respace_stuffed`
  (растянуть скомканный прогон по дыре пропорц. скелету; старт = CTC-позиция куска, но не раньше конца
  пред. слова → пред. слово держит фокус сквозь مadd, потом подсветка едет по куску). Опт-аут
  `SYNC_FALIGN_RESPACE=0`. П8 кормится сырыми bounds (`raw_bounds`) — не задет. Проверено rec11 (GPU 12с):
  53:4 wi0=17.48…wi4=20.16, الْهَوَىٰ держится 13.48→17.48 (~4с ✓), дыра убрана; `respaced=11 dropped=0
  repeats=6`. Все записи переgenerированы через сервис — регрессий нет (rec1/5/6/7/9/10 `respaced=0`,
  rec7 خضرا repeats цел). Детали — BACKLOG «Известные баги» (✅ верх). **v2 по фидбегу:** идея (2) —
  явный детект held-гласной по RMS; (3) char/харакат-уровень. Ждём замечаний владельца с новых ссылок.
- **Сделано 11.07 (сессия 9): П8 over-reach rec11 0:50 — РЕШЕНО (схлопывание тандема декода).** Владелец
  дал 3 таймкода (0:50, 01:24, 01:58). Инструментированный диагноз (объективно, без звука): на 0:50 дыра
  53:12 декодилась удвоенно (`kthblfdmr·kthblfdmr·ftmrwnk` — чтец без паузы перечёл 53:11 дважды) →
  length-biased Левенштейн тянул назадний матч на ЛИШНИЙ аят (53:10 вместо 53:11). Фикс:
  `falign._collapse_tandem` схлопывает тандем в начале декода → матч `[11:1..12:0]` (возврат на 53:11).
  Валидация: все 7 записей перегнаны — rec11 17→11 точек (ушли ложные 53:10), rec7 خضرا цел (repeats=4),
  rec1/5/6/9/10 0 ложняков. **01:24 и 01:58 — НЕ баги детектора:** 01:24 = вдох в паузе (декод `th`),
  01:58 = само-повтор إِنْ (декод `n`, некуда прыгать). Детали — BACKLOG «Известные баги» (✅ верх).
  (Замечания владелец обещал записывать в `new_tasks/` — проверяй папку при спавне; файлы после
  разбора в беклог УДАЛЯЙ, папку НЕ трогай.)
- **Сделано 11.07 (сессия 9, продолжение):** (1) устранил ПРОТИВОРЕЧИЕ в беклоге по rec11 0:50 —
  старая кросс-аятная запись помечала возврат на 53:10 как «как слышит владелец», но владелец затем
  ухом дал over-reach → верно 53:11 (тандем-фикс). Пометил, чтобы future-me не откатил `_collapse_tandem`.
  (2) `tools/audit_repeats.py` — объективный аудит возвратов П8 БЕЗ звука (читает forced sync-map'ы,
  печатает событие: остановка→назад→вперёд + mm:ss, ⚠ на дикие прыжки). Прогон: 0 диких прыжков, rec11
  0:57 ровно на 53:11 (фикс подтверждён в live run.data), rec7 خضرا цел (4), rec1/5/6/9/10 чисто.
  Юзать при новых таймкодах владельца. **Всё ещё ждём ЕГО вердикт по слуху на 0:50.**
- **Сделано 10.07 (сессия 7):**
  - ✅ **проверка перемолки rec1/5/6/7/9 (памятка с 04.07) — ЗАКРЫТА.** Все `ready`, ошибок нет;
    у ASR `metrics.match` (wer), у forced `stuffed_dropped=0`, coverage 0.977–1.0. BACKLOG п.5.
  - ✅ **Next п.5(б): подтяжка границ слов к тишине** — `falign._snap_bounds` (RMS-огибающая, порог
    от шумового пола+гистерезис, поджатие ТОЛЬКО внутрь к речи, опт-аут SYNC_FALIGN_SNAP=0). rec5:
    49% слов поджато, медиана 100/80мс, 0 новых вырожденных, coverage 1.0. `meta.snapped_to_silence`.
  - ✅ **forced align НА GPU (владелец: «не хочу минуту молотьбы на 10-мин аудио»).** onnxruntime→
    onnxruntime-gpu 1.20.1 (CUDA12/cuDNN9 — тот же стек, что ct2; ORT≥1.27 хочет CUDA13, НЕ брать)
    + CUDA-колёса (cuda_runtime/cufft/curand/nvrtc/nvjitlink) + LD_LIBRARY_PATH в Dockerfile. Образ
    пересобран. **rec10 (654с) = 17.6с на GPU против ~6 мин CPU.** `batch_size=1` (env
    SYNC_FALIGN_BATCH): batch≥2 падал OOM на 6ГБ RTX даже при свободной VRAM (арена onnxruntime).
- **Разъяснено владельцу 10.07 (важные концепты, могут всплыть):** (1) forced НЕ ищет по всему
  Корану — диапазон аятов уже дал ASR; жрёт CPU/GPU нейросеть-акустика MMS (forward-pass по всему
  аудио), а НЕ поиск и НЕ выравнивание (DP дешёвый). (2) forced монотонен → повторы не выражает
  В ПРИНЦИПЕ (это П8, отдельный шаг, НЕ «настройка» forced). (3) Идея владельца (в NOTES): MMS-эмиссии
  можно декодить сами → искать отрывок в Коране → БЕЗ google/whisper и align.py («свой распознаватель»,
  целевая архитектура (A), после П8). (4) UI: тумблер прогонов в плеере спрятать за ?debug=1 (мелочь).
- **NB инфра:** порт 8000 на хосте часто занят ЧУЖИМ проектом (`wildbox` в PyCharm) → локально `web`
  поднимается на **8002** (gitignored `docker-compose.override.yml`, memory [[local-web-port-8002]]);
  в репо `docker-compose.yml` стандартный 8000. Для работы с БД без веба: `docker compose exec worker
  python manage.py shell`. Скрипты в контейнере: `sys.path.insert(0,'/app/src'); sys.path.insert(0,'/app/service')`.
- **Сделано 04.07 (сессия 6, поздний фидбек владельца): список записей — живое обновление без
  `location.reload()`** (страница перезагружалась каждые 2.5с при пересчёте — «картинки мигают,
  бесит»). Карточка → партиал `_card.html` + `GET /r/<id>/card`; JS точечно подменяет одну
  карточку при смене статуса, `<img>` переносится узлом (0 перекачек). Детали — BACKLOG
  «Сделано». Мониторинг перемолки: rec1/5 домолоты, rec6 в работе, rec7/9 в очереди —
  проверка метрик по памятке ниже всё ещё АКТУАЛЬНА.
- **Сделано 04.07 (сессия 6): разобрана указка `NEW_TASKS/` — репо cpfair/quran-align.**
  Владельца интересовал сам аллайнер + повторные чтения (П8). Изучены все исходники →
  `docs/RESEARCH-quran-align.md`. Итог: как есть не берём (не turnkey, CMU Sphinx 2016);
  повторы он НЕ решает, но DP-рамка правильная. **Наследуем:** правило плотности 100мс/слово,
  подтяжку границ к тишине, ins/del/transp-метрики (BACKLOG Next п.5) и ГЛАВНОЕ — готовый
  план П8 (повторы поверх плотного tarteel-ASR, фронт не трогаем; тест rec6 خضرا 1:24/1:29).
  П8 поднят владельцем повторно («мои чтецы часто так делают») — приоритет вырос.
  Реализованы (а)+(в) из Next п.5 + 2 попутных бага сервиса (refresh forced после пересчёта ASR;
  источник forced = лучший по coverage, не фикс-google) — rec10 сквозняком: 0.742→0.985.
  **П9 (новое):** владельцу не нравится текущий вид арабского, хочет турецкий мусхаф; усмани-файл
  Tanzil скачан в `data/quran-uthmani.txt` (он глянет). NB: усмани = мединский расм, а турецкие
  мусхафы — имляи; наш quran.db уже имляи → скорее вопрос шрифта/вёрстки. Ждём вердикта.
- **Сделано 04.07 (сессия 5): tarteel ПОДКЛЮЧЕН (закрыл п.8).** `whisper-base-ar-quran` → ct2
  (`./cache/ct2-tarteel-base`, gitignored), env `SYNC_WHISPER_MODEL` в `src/asr.py`, воркер грузит на
  GPU по умолчанию (compose). **Ключевой фикс:** silero-VAD ВЫКЛЮЧЕН по умолчанию (`SYNC_ASR_VAD=1` —
  опт-ин) — на таджвиде VAD принимал распев за тишину, выкидывал ~97% аудио (rec9: с VAD 5 слов, без 896;
  било ЛЮБУЮ модель). Честная перемолка через сервис: whisper теперь сопоставим с google/forced
  (rec1 6→906 сл cov 0.954, rec9 5→876, rec6 74→992, rec10 0→334 обошёл google 188). Детали+таблица — BACKLOG п.8.
- **⏳ ПРИ СЛЕДУЮЩЕМ СПАВНЕ ПЕРВЫМ ДЕЛОМ:** 04.07 ~23:30 запущена перемолка rec1/5/6/7/9 через
  сервис (ASR в очередь + forced refresh; владелец закрыл сессию, докер домалывает сам,
  ~полчаса-час). ПРОВЕРИТЬ результаты: у ASR-прогонов должен появиться `metrics.match` (wer),
  у forced — `stuffed_dropped` и источник по лучшему coverage (на rec7 сменится google→whisper);
  сверить coverage до/после (до: rec1 f=?, rec5 1.0, rec6 1.0, rec7 1.0, rec9 ?; rec10 уже
  проверен: 0.742→0.985). Если что-то в error — разобраться. Потом обновить BACKLOG п.5 и
  отчитаться владельцу в ответе.
- **Сейчас в работе / СЛЕДУЮЩЕЕ (после проверки выше):** П8 повторы чтеца по готовому плану
  (`RESEARCH-quran-align.md`, тест rec6 خضرا 1:24/1:29) · остаток Next п.5(б) — подтяжка границ
  к тишине · П9 — ждёт вердикта владельца по `data/quran-uthmani.txt` (турецкий мусхаф = скорее
  шрифт/вёрстка, наш текст уже имляи) · комплексная coverage-метрика (🟡 «Позже») · фронт на
  реальном телефоне.
- **Сделано 04.07 (сессия 4):** (1) **GPU-докер заведён как часть пайплайна** — владелец поставил
  `nvidia-container-toolkit` (sudo), `docker compose up -d --build` пересобрал web+worker; в воркере
  `ctranslate2 cuda devices:1`, `falign.available():True`; все 5 записей перемолоты ЧЕРЕЗ СЕРВИС
  (dispatch→celery→worker, whisper large-v3 на GPU ~5-7с/запись при кэше в `./cache`, forced авто-пост-шаг).
  Конец хостовым костылям. (2) **Починена coverage-метрика** (владелец заметил: whisper 6 слов, а cov 1.0!):
  старая была самореферентной (aligned/asr_words) → headline `coverage` теперь = доля ДЛИТЕЛЬНОСТИ АУДИО,
  покрытая словами (`pipeline._audio_time_coverage`, бины 10с, знаменатель = реальная длительность →
  сравнимо между распознавателями). Старое соотношение → `metrics.aligned_ratio`. ⚠️ Метрика ещё НЕ
  идеальна (длина аудио тоже кривой знаменатель: паузы/вступления/темп чтеца) — доработать до комплексной,
  занесено в BACKLOG «Позже» как 🟡 (НЕ закрыто).
- **Раньше (сессия 3):** docker-образ с GPU подготовлен (`service/Dockerfile`: torch CPU-колесо +
  ctc-forced-aligner/onnxruntime/Unidecode + nvidia-cudnn/cublas, `LD_LIBRARY_PATH` на pip-cuDNN,
  g++ для сборки C++-расширения ctc-forced-aligner, `HOME=/app/.cache`+`HF_HOME`; `docker-compose.yml`:
  worker `deploy.resources.reservations.devices` nvidia, bind-mount `./cache`).
- **Сделано 04.07 (сессия 2, голос 08:39):** (1) whisper теперь ТОЛЬКО GPU — CPU-фолбэк выпилен
  из `src/asr._load_model` (на арабском CPU даёт кашу, 5 слов на 20 мин). `SYNC_ASR_DEVICE=cpu` — опт-ин.
  (2) forced выведен из «распознавания» в UI: player.html развёл «распознавание:» (google/whisper) и
  «выравнивание:» (forced), флаг `AsrRun.is_aligner`. (3) docker web/worker от `user 1000:1000` —
  контейнеры пишут media/work/db от UID хоста, не root. (4) rec9 перемолот (whisper-GPU+forced+google).
  Ресёрч Tarteel → `docs/RESEARCH-tarteel-asr.md`.
- **Сделано 04.07 (сессия 1):** add-by-link (`forced` — авто-пост-шаг, не выбор), Google-креда в
  web+worker (`./creds/` gitignored), фронт-мелочи 0b, метаинфо П6, уникальность/поиск П7. Скрины — ок.
- **ВАЖНО — хост-python:** перемолка на хосте (whisper-GPU/forced) — только через `/usr/bin/python3`
  (system 3.12 с `~/.local/lib/python3.12/site-packages`), НЕ `python`/`python3` (перехватывает pyenv,
  там нет 3.12 → exit 127). Плюс `export LD_LIBRARY_PATH=…/nvidia/cudnn/lib:…/cublas/lib` для GPU-whisper.
- **Договорённости:** аудио-ТЗ (`.ogg` + транскрипты) складываю в `media/` (они как ТЗ, трекаются
  в git через whitelist в `.gitignore`). Разбор голосовых ТЗ раскидываю по BACKLOG/TZ/NOTES.
- **Сделано 11.07 (сессия 10, remote-control через Telegram):** (1) записано правило самокомпакта
  (голос владельца) — `/self-compact` после КАЖДОГО закрытого пункта беклога (пуш+записи), ритм
  ~15–20 мин, мелкие пункты <15 мин накапливать до следующего; зафиксировано в «Режим работы» выше +
  memory `self-compact-cadence`. (2) Синхронизированы статусы П13/П14 в таблице требований (были 🔴,
  по факту 🟢 в `64b8ba2`). (3) **Остаток П11 — арабская нормализация клиентского поиска:** `arNorm`
  в `static/index.html` (порт бэкового `_ar_norm`), паритет с Python байт-в-байт, browser-тест 5/5.
  Остаток П11 теперь только деплой статики на GitHub Pages (ждёт решения по боевому `?api=`).
- **Сделано 11.07 (сессия 10): П12 — ручной элайнер, база v1.** `service/recitations/static/manual.html`
  (статик, как плеер/библиотека) + роут `/r/<id>/manual` (`views.manual` редирект на статику) +
  ссылка «✍ ручной» в отладочной панели плеера (`?debug=1`). Фетчит `data.json` → аудио + слова
  Tanzil (канон wi). Клавиатура рулит аудио (мышь занята вводом): Space=пауза, ←/→ ±5с, Shift+←/→
  ±10с. Мышь по словам (`mouseover`→`hoveredKey`) пишется против аудио-времени в `tick()` (rAF):
  открытый интервал ведётся, смена слова/пауза/выход курсора закрывают его. После перемотки 1с
  заморозка (❄, `freezeUntil` wall-clock). **Overwrite-merge** (`commit`): новый интервал `[a,b]`
  обрезает/раскалывает пересекающиеся старые → переиграл кусок = перезапись только его. Экспорт →
  `word_timeline` JSON (совместим с плеером/build_data), Копировать/Скачать. Работает поверх записи,
  где текст/диапазон уже есть (data.json готов) — дебаг/коррекция. Проверено playwright 8/8
  (merge/split/сплит-охвата/запись мышью), `manage.py check` ок. **v2 (ждёт фидбек владельца):**
  сохранение как прогон в бэк (AsrRun manual) вместо download; выбор диапазона аятов С НУЛЯ (без ASR);
  видео-источник; клавиши-настройки. Детали — BACKLOG таблица П12.
- **Сделано 11.07 (сессия 11): П12 — предзагрузка привязки в ручной элайнер (коррекция вместо
  разметки с нуля).** `manual.html` при открытии заливает `word_timeline` прогона из data.json как
  стартовые интервалы (`seedFromData`) → правишь мышью уже выровненную запись (overwrite-merge), а не
  с чистого листа — это и есть корректирующий сценарий v1, что описывал владелец. Кнопка «↻ из прогона»
  перезаливает, «Очистить» даёт пустой лист. У ASR-прогонов без `t_end` конец слова приближаем зазором
  до следующей точки (потолок 0.6с `_SPAN_CAP`, как бековый `_WORD_SPAN_CAP`). Чисто клиентское — бэк/
  реестр/модель НЕ тронуты (не путать с v2-сохранением в бэк, оно ждёт фидбек владельца). Проверено
  playwright 12/12 (4 новые: 4 интервала, wi0 [0,0.8], wi2 без t_end→потолок 2.2, .done на словах),
  node --check ок. Коммит+пуш.
- **Сделано 18.07 (сессия 14, голос tg_520/525): WI — инвариант чтеца + гард от прыжков вперёд.**
  Владелец на rec7 (1:29–2:00): подсветка рвано прыгает вперёд/назад, пропускает слова; требовал
  проверки АЛГОРИТМИЧЕСКИ в коде (не ручными прогонами). Правило: вперёд ТОЛЬКО по одному, назад —
  возврат, прыжка вперёд через слова не бывает. (1) `pipeline.alignment_invariants` — детект по
  плоскому индексу (соседи по t: +1 или ≤0, не ≥+2), в `metrics.forward_jumps` на каждом прогоне +
  `tools/audit_alignment.py`. Вскрыло: forced/w2v прыжки ТОЛЬКО на возвратных rec7/11/12 → баг в
  возвратах (детектор брал лишь начало перечитки, back=21..21, дорожка прыгала 21→27). (2) Пост-гард
  `pipeline.fill_forward_jumps` — финальный слой (не конфликтует со слоями, идёт последним): прыжок
  2..14 слов заполняется пропущенной перечиткой равномерно → плавный пробег. forced/w2v прыжки rec7
  3→0, rec11/rec12 →0. Backfill без GPU. **Плюс вернул выбор прогона** (тумблер в плеере был за
  ?debug=1 → виден всегда; чипы на листе кликабельны). Коммиты в main. **Остаток:** корень возвратов
  (недобор перечитки в `_detect_repeats`) — заполнение лечит симптом; ASR-межсурные прыжки отдельно.
- **Сделано 18.07 (сессия 14, голос tg_520 «facepalm»): честные фиксы возвратов по АКУСТИКЕ.**
  Владелец: пост-гард fill (заполнение прыжков) = ЗАМАЗКА (рисовал будто пропущенные слова читаются)
  → ОТКАЧЕН. Диагноз по акустике (greedy-CTC декод MMS-эмиссий vs скелеты слов, эмиссии в
  `work/rec7_emissions.npy`): 2 из 3 возвратов rec7 ЛОЖНЫЕ — forced-времена в зонах мадда разъезжаются,
  детектор ловит мусор (на 2:01 звучит زيتون wi27, матчит قنوان wi22). Взамен — 3 честных фикса:
  (1) `falign._REPEAT_ZONE_HEALTH`=0.35 (r=2) — акустическая валидация: возврат принять только если
  зона вокруг здорова (доля forced-слов, чей декод бьётся со скелетом; خضرا 0.40 vs ложные 0.0-0.2);
  (2) добор перечитки `rb=max(rb,i)` — перечитка идёт до слова ухода (убирает прыжок 12→14);
  (3) `models.active_run` — дефолт по МИН `forward_jumps` (rec11 → forced чистый, остальные → w2v).
  Итог: forced fj=0 на ВСЕХ 9; w2v fj=0 кроме rec11 (кросс-аятн зигзаг — остаток). НИКАКОЙ замазки.
  **Инструмент диагностики: декод акустики окнами + матч со скелетами (в shell worker, эмиссии .npy).**
  **⚠️ Грабли: прерывание shell по таймexc-у оставляет осиротевший gpu_align, держит VRAM → CUDA OOM
  следующим. Чистить: kill gpu_align в контейнере (grep /proc/*/cmdline). Перегонять w2v ПО ОДНОМУ.**
- Обновлено: 2026-07-25 (сессия 16 — свежая указка владельца: выкинуть деление аллайнер/распознаватель,
  плоские независимые плагины + общий match_align, референс analytics_notifications подтверждён; спека
  и арх-план актуализированы; rec7 честно продиагностирован — FWD-возврат работает, дефект №1 отклонён
  before/after, остаётся дрейф на распеве; СЛЕДУЮЩЕЕ — рефакторинг Task #2 по плану выше. Ниже — сессия 15:)
- Обновлено ранее: 2026-07-25 (сессия 15 — w2v ПОЛНОСТЬЮ независим: whisperx выкинут, свой CTC-Viterbi +
  find_range (9/9 диапазонов — rec10 Ар-Рахман закрыт 2-м сидом «целая сура пика» по реальному ratio)
  + ВОЗВРАТЫ по своей акустике (Task #3, детект по РАСТЯНУТОМУ span, не дырам; FWD/BACK; пост-гард
  fj=0 отбраковкой групп). ВСЕ 9 рек с полноценным w2v — ДЕФОЛТ везде, fj=0 везде, диапазоны верны,
  cov 0.914–0.998 (rec5..14 reps=8/9/8/21/4/20/14/12/8). rec11 зигзаг ЗАКРЫТ пост-гардом (fj=0,
  w2v дефолт). rec7 кейс владельца: خضرا+صاحبة ×2. **Владелец должен послушать rec7 (и rec9/rec11 —
  reps=21/20, высокие) — reps-точность единственное, что нельзя провалидировать без его слуха.**

## Глоссарий (термины владельца)

- **«рек» / «река»** — местный жаргон владельца для **recitation** (запись чтения = `Recitation`).
  «rec7», «дай реке прежний id» и т.п. — это про записи чтения, не про что-то ещё.

## Режим работы

- **⏰ ПРИ СТАРТЕ СЕССИИ вооружай само-будильник: вызови скилл `moloti`** (`/moloti`, дефолт 20 мин).
  Он ставит фоновый Monitor, который раз в N минут будит меня пинком `moloti_nudge` (даже если я
  «остановился») → на пинке перепроверяю usage+контекст и, если не было прямого приказа стоп, беру
  верхний пункт беклога и молочу дальше. Владелец крутит период на лету: `/moloti 10`; гасит `/moloti
  off`. Детали и обработка пинка — в самом скилле (`.claude/skills/moloti/SKILL.md`). На компакт-
  резюме Monitor обычно ещё жив → повторный `/moloti` просто подтвердит (дубля нет, singleton по flock).
- **Читай `docs/BACKLOG.md` ПЕРВЫМ.** Там актуальные статусы, «Next» и указки владельца.
- **Работаем автономно, без переспрашиваний.** Владелец часто отсутствует, даёт короткие указки
  (нередко голосом `.ogg` → skill `audio-task`) и периодически говорит «продолжай развитие».
- **Коммить И ПУШИТЬ на каждом завершённом шаге / логической точке — и продолжай.** Коммит и пуш
  ВСЕГДА вместе, автоматически, без подтверждений. Не жди разрешения, не копи гору изменений.
  Дошёл до осмысленного состояния → `git commit` с внятным сообщением → `git push` → дальше.
- **`/self-compact` после КАЖДОГО закрытого пункта беклога** (голос владельца 11.07): когда пункт
  реально завершён — код готов, `git commit`+`git push` сделаны, И все записи обновлены (BACKLOG
  прогресс, «Немедленная память», memory) → делай `/self-compact` и продолжай. Ритм ~15–20 мин
  активной работы. НЕ компактить на середине задачи — только на завершённой логической точке.
  Если пункт закрылся быстрее ~15 мин — НЕ компактить сразу, накопи и сделай `/self-compact`
  после следующего закрытого пункта (не дробить компакты на мелочь).
- **ВСЕГДА МОЛОТИ. Не останавливайся спрашивать «что дальше».** Закончил задачу → коммит+пуш →
  СРАЗУ берёшь следующий пункт. Нет явной указки / не осталось (по-твоему) важного — бери
  **следующий пункт из `docs/BACKLOG.md`** (верхний невыполненный из «Next»/🔴/🟡) и делай.
  Простой в ожидании вопросов владельца = ошибка. Останавливаться можно только когда реально
  нечего взять из беклога (тогда — предложить/наполнить беклог), либо нужен ввод, который есть
  ТОЛЬКО у владельца (напр. фидбек с реального телефона).

## Приоритизация (это важно — я веду проект сам)

- **Не переключайся слепо на последнее сообщение владельца как на единственную/немедленную задачу.**
  Его сообщение — это вход в приоритизацию, а не приказ бросить всё и делать именно это сейчас.
  Я сам расставляю порядок работ.
- **Приоритет №1: сделать КОР-ЛОГИКУ полноценно и правильно, а не «прототипно».** Указка владельца
  24.07 (жёстко, капсом): «ЗАБУДЬ, ЧТО МЫ ДЕЛАЕМ ПРОТОТИП. Без кор-логики не будет самого сервиса.
  ДЕЛАЙ СРАЗУ ВСЁ НОРМАЛЬНО, даже если нудно и долго. Не стопь сам себя.» Из этого следует:
  - **закапываться в детали кор-логики (выравнивание, распознавание, возвраты) — НОРМА и нужно.**
    Глубокие ресёрчи, харакат-левел, char-level, свой распознаватель из акустики — делать по-настоящему,
    а не откладывать «на потом ради каркаса». Каркас уже есть; теперь качество ядра.
  - НЕ упрощать ради скорости там, где это роняет корректность синхрона. Лучше долго и правильно.
  - «понятно что плохое» — чинить, а не заносить в беклог и обходить.
- **Всё новое всё равно фиксируй в `docs/BACKLOG.md`** (идеи, баги, указки владельца) — плюс
  `NOTES.md`/`TZ.md` по месту. Цель: при следующем спавне помнить всё и продолжить с места.

## Прочее

- **Ключ Google STT — ТОЛЬКО через env `GOOGLE_APPLICATION_CREDENTIALS`.** Никогда не коммитить
  в репо (утечёт на GitHub).
- Ядро (`src/`: ingest→asr→align→build) держать тонким и импортируемым; оркестрацию/хранилище/API —
  в Django-сервисе (`service/`).
