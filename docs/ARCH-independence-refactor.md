# РЕФАКТОР: независимые источники + общий модуль матчинга (директива владельца 24.07)

> **Голос владельца 24.07 (резко, по делу).** Проблема: `w2v` наследует возвраты из `MMS`, `MMS`
> берёт диапазон из матчинга, матчинг — из `whisper`/`google`. «Империя на кривых распознавателях».
> Сравнивая прогоны, владелец думал, что смотрит РАЗНЫЕ подходы — а по сути один и тот же, где косяки
> одного затыкаются другим, набирая новые. **Требование: каждый источник ПОЛНОСТЬЮ независим,
> общая логика — в один переиспользуемый модуль, без наследования результатов. Проще, меньше слоёв.**

## Целевая архитектура

- **Источник (source)** — самодостаточный модуль: `audio → своя акустика → буквы/слова с временами`.
  Их РАВНОПРАВНЫЙ массив (никаких «типов» распознаватель/аллайнер):
  - `google` — Google STT → слова.
  - `whisper` — faster-whisper (tarteel ct2) → слова.
  - `mms` (быв. forced) — MMS-CTC → романизованные буквы/слова.
  - `w2v` — wav2vec2 (whisperx) → арабские буквы/слова.
  - `manual` — человек мышью (особый: истина).
- **Общий модуль `match_align`** (переиспользуется ВСЕМИ, не наследуется): `буквы/слова+времена +
  quran.db → sync_map` (какой аят где = матчинг к Корану + выравнивание слов + детект возвратов +
  snap к тишине). Один код, вызывается из каждого источника внутри. Сегодня это ЧАСТЬ `src/align.py`
  (матчинг ASR→Коран) — обобщить и на буквенный вход (MMS/w2v), чтобы аллайнеры сами определяли
  диапазон из СВОЕЙ акустики, а не брали у ASR.
- **Никаких кросс-зависимостей между источниками.** Каждый гоняется от аудио до word_timeline сам.
  Сравнение прогонов = сравнение РЕАЛЬНО разных подходов.

## Что сейчас нарушает (карта зависимостей)

- ✅ `pipeline._inherit_repeats` — w2v тянул возвраты из MMS. **УДАЛЕНО** (сессия 17).
- ✅ `pipeline._forced_source` — forced брал ДИАПАЗОН аятов из готового google/whisper. **УДАЛЕНО**
  (сессия 17, шаг 3): forced сам находит диапазон из своей акустики MMS (романизованный
  find_range), гейт `ready()` снят.
- ✅ `recognizers.ALIGNERS`/`is_aligner` + `tasks._maybe_forced`/`_maybe_w2v` — деление на типы и
  авто-пост-шаги. **УДАЛЕНО** (сессия 17): плоский пакет `sources/`, каждый источник запускается сам.
- ✅ `models.active_run` — было по типам/множеству ALIGNERS. Переведено на `sources.keys()`/`is_aligned`.

## Что УЖЕ хорошо (переиспользовать, не ломать)

- `src/align.py::align(transcript, quran)` — матчинг слов к Корану + сборка word_timeline. Это ядро
  общего `match_align` для СЛОВНОГО входа (google/whisper уже независимы через него).
- `falign._snap_bounds`, `_detect_repeats` (акустика), `_greedy_ctc` — кирпичи выравнивания/возвратов,
  переносятся в общий модуль.
- Возвраты на самоподобии аудио (проверено 24.07, дискриминирует повтор 0.26 vs норма 0.07–0.13) —
  универсальный детект возвратов, НЕ завязан на конкретную акустику → в общий модуль.

## План (инкрементально, каждый шаг — рабочее состояние + коммит)

1. **w2v независим по возвратам** (текущая боль rec7): свой детект возвратов на самоподобии его же
   аудио в `w2v_align`; убрать `_inherit_repeats`. Проверить rec7 (وخلق كل شيء ×2 едет), регрессии
   rec11/خضرا. ← начато 24.07.
   **Статус прототипа (24.07):** самоподобие MFCC НАДЁЖНО находит ЗОНУ повтора (rec7 101: sim 0.34
   vs норма <0.13; خضرا тоже) и не ловит обычные разрывы — независимость доказана. НО точная
   СТРУКТУРА груба: период L=3.48с × разрыв 12.4с → n≈4 копии, хотя владелец слышит ×2, и фраза
   захватывает лишний وهو(101:14). Причина: период самоподобия смешивает перечитку وخلق с началом
   продолжения. **Не шипить грубое (владелец не терпит кривизны).** ДОРАБОТКА: уточнять счётчик n и
   границу фразы через wav2vec2 force-align кандидатов (phrase×2 vs ×3 + continuation, брать по
   avg-score — в прототипе 2× score 0.565 > 1× 0.347, ранжирование работает). Плюс: خضرا сейчас
   держится на УНАСЛЕДОВАННЫХ из MMS точках → тестить детект на ЧИСТОМ w2v (без inherit), убедиться
   что ловит сам, ИНАЧЕ регрессия при удалении `_inherit_repeats`. Порядок: довести детект до точного
   на rec7(101)+خضرا+rec11(53:53) → потом убрать inherit. Самоподобие — CPU (без GPU/OOM).
2. **Обобщить `align.py` матчинг на буквенный вход** (романизованные MMS-буквы / арабские w2v-буквы →
   какой аят где), чтобы аллайнер определял диапазон сам. Вынести в общий модуль `match_align`.
3. **MMS/forced независим:** MMS-эмиссии → буквы → match_align (свой диапазон) → align → возвраты.
   Убрать `_forced_source` для него.
4. **Уплостить модель источников:** единый массив, снять `ALIGNERS`/типы/`_maybe_*`; `active_run`
   упростить (выбор по метрикам, напр. min forward_jumps + coverage).
5. **Чистка:** удалить мёртвые кросс-зависимости, слои. Прогнать все записи, аудит инвариантов.

## Принцип-образец (владелец 24.07): плагины-по-соглашению + динамическая подгрузка

Владелец видел на другом своём проекте удачную структуру и предложил ПРИНЦИП (не копировать чужую
специфику — подходит ли, решаем отдельно; но для новой структуры полезно):

- **Папка с файлом на каждый тип.** Каждый тип-плагин — отдельный самодостаточный модуль, внутри —
  только его специфика.
- **Единый контракт ПО СОГЛАШЕНИЮ** (не базовый класс, не реестр): каждый модуль экспонирует
  функции с одинаковыми именами/сигнатурами. Пример из того проекта — уведомления, где каждый тип
  давал две функции: «посчитать, кого уведомить» и «отрендерить текст».
- **Динамическая подгрузка по имени**, вызов единообразный:
  ```python
  mod = importlib.import_module(f'{PKG}.{name}')   # name — имя типа
  result = mod.do_step(...)                         # одинаково для всех типов
  ```
- Новый тип = новый файл с теми же функциями. Никакой проводки. Duck-typing по имени модуля.

**Как это МОЖЕТ лечь на источники синхрона (гипотеза, проверить пригодность):** папка `sources/`,
модуль на источник (`google.py`/`whisper.py`/`mms.py`/`w2v.py`/`manual.py`), каждый — фикс-контракт
(напр. `recognize(audio) -> units`), лоадер грузит по имени, `pipeline` итерирует список. «Типов»
(aligner/recognizer) нет. Сегодня `recognizers.REGISTRY` — зачаток, но логика размазана по
`asr.py`/`falign.py`/`w2v_align.py`/`align.py`/`pipeline.py` с кросс-связями → в модули-источники.

### ✅ ПОДТВЕРЖДЕНО 25.07 (сессия 16): владелец дал точный референс (tg_1958) — паттерн применим

Владелец указал конкретный код: **`~/work/wildbox/airflow-backend-analytics/dags/analytics_notifications`**.
Изучен. Паттерн ровно тот, что нужен:
- `subscriptions/` — по файлу на тип (`cpa.py`, `cpm.py`, `crr.py`, `romi.py`…), каждый самодостаточен.
- **Единый контракт по соглашению** (не базовый класс): каждый файл экспонирует `get_to_notify(session,
  users_by_cabinet_id, date_from, date_to)` + `format_to_text(user_setting, data, cabinet_name)`.
- **Динамический импорт по имени:** `main.py:100` → `importlib.import_module(f'.subscriptions.{field}',
  __package__)`; снаружи вызов ЕДИНООБРАЗНЫЙ (`subscription_module.get_to_notify(...)` /
  `.format_to_text(...)`) — оркестратор про внутренности типа не знает. Новый тип = новый файл.

**Ложится на источники так:** пакет `service/recitations/sources/` (или `src/sources/`), файл на источник
(`google.py`/`whisper.py`/`w2v.py`/`mms.py`/`manual.py`). Контракт по соглашению:
```python
KEY, LABEL, NOTE = "w2v", "…", "…"          # метаданные (заменяют recognizers.REGISTRY-запись)
def available() -> bool: ...
def run(audio_path, quran, ctx) -> dict:    # audio → своя акустика → sync_map {meta,timeline,word_timeline,[char_timeline]}
    ...                                     #   внутри зовёт ОБЩИЙ match_align, свой матчинг не пишет
```
Лоадер (`sources/__init__.py`): `pkgutil.iter_modules` → динамически собирает плоский массив по KEY.
Убирает `recognizers.ALIGNERS`/`is_aligner`/`selectable_recognizers` и ветвление `_maybe_forced`/
`_maybe_w2v`/`_recognize` — `pipeline` просто итерирует `sources` и зовёт `run` одинаково.

### Статус плана (обновлено 25.07)

- **Шаг 1 (w2v свои возвраты, убрать inherit):** ✅ СДЕЛАНО — `w2v_repeats.detect` (span, FWD/BACK,
  пост-гард fj=0); `_inherit_repeats` убран из пути вызова, а код-заглушка удалена в шаге 4/5 (сессия
  17). w2v дефолт на всех 9, диапазоны верны (кейс F закрыт), cov 0.914–0.998.
- **Шаг 2 (общий `match_align`, буквенный вход):** ✅ СДЕЛАНО (сессия 17). Создан `src/match_align.py` —
  ЕДИНЫЙ модуль матчинга к Корану с двумя входами: СЛОВЕСНЫЙ (`align`/`load_transcript`/`match_stats`/
  `CorpusIndex`/`Word` — из align.py) и БУКВЕННЫЙ (`find_range`/`build_index`/`greedy_skeleton`/
  `ayah_start_hints`/`ctc_logprob`/… — из w2v_range.py). `align.py` и `w2v_range.py` теперь тонкие
  ре-экспортные шимы над ним (pipeline/run/gpu_align/офлайн-пробы НЕ тронуты). Поведение-сохраняюще:
  OLD==NEW байт-в-байт (сверка find_range на rec7-эмиссиях), словесный align rec11 cov 0.964/rec9
  вменяем, `find_range` с сохранёнными декодами 9/9 диапазонов (rec7 6:95-103), `manage.py check` чист.
  NB: свежий greedy-декод кеша rec7_w2v_emis.npy даёт иной диапазон (13:35) — свойство decode-источника,
  НЕ match_align (идентично старому коду); e2e-путь берёт декод из эмиссий live и даёт верный 6:95-103.
- **Шаг 4 (уплостить модель источников, плагин-пакет):** ✅ СДЕЛАНО (сессия 17). Создан пакет
  `service/recitations/sources/` — плоские независимые плагины (файл-на-источник), лоадер
  `sources/__init__.py` (`pkgutil.iter_modules`+`importlib`, реестр по PRIORITY). Пять источников:
  `manual`(P5)/`w2v`(P10)/`forced`(P20)/`google`(P30)/`whisper`(P40), каждый объявляет
  `KEY/LABEL/NOTE/SELECTABLE/AUTO/ISOLATE/ALIGNED/PRIORITY` + `available()/ready()/run(rec,audio,quran,
  out,stage)`. **Снято:** `recognizers.py` целиком удалён (REGISTRY/ALIGNERS/is_aligner/PRIORITY/
  selectable_recognizers); `tasks._maybe_forced`/`_maybe_w2v` → обобщённый `_ensure_auto` (перебор
  `sources.auto()`); `pipeline._recognize` (google/whisper-ветки → `sources/google.py`,`whisper.py`);
  ветвление `run_one` по `is_aligner` → единый диспетчер (ISOLATE→подпроцесс gpu_align, иначе in-proc);
  `gpu_align.py` обобщён (грузит модуль по ключу, зовёт `mod.run()`, без W2V/else-веток). `active_run`
  через `sources.keys()`/`is_aligned` (логика та же: manual > min-fj среди ALIGNED > PRIORITY-ASR).
  views/models переведены на `sources`. **Поведение-сохраняюще (e2e на rec5):** google (in-proc)
  cov 0.977 fj0, w2v (ISOLATE-подпроцесс) cov 0.914 fj0, forced cov 0.478 fj0 — word_timeline
  байт-в-байт идентичен старому; `run_single`→`_ensure_auto` гонит w2v+forced, VRAM освобождается
  (15 MiB), 0 осиротевших gpu_align; `manage.py check` чист; реестр 5/5. Инструмент — `work/verify_match_align.py`.
- **Шаг 3 (MMS/forced независим, свой диапазон):** ✅ СДЕЛАНО (сессия 17, коммит `3298a69`). forced
  сам находит диапазон из своей акустики MMS, `_forced_source`+гейт `ready()` сняты. Как: `find_range`
  сделана алфавит-агностичной (общее ядро `_index_over(quran, tok_str, k)`); добавлен
  `build_romanized_index` — двойник индекса в РОМАНИЗОВАННЫХ согласных скелетах (uroman+unidecode),
  тот же алфавит, что MMS-greedy-декод. Романизованный путь: `k=_K_ROM=7` (~20 латинских согласных →
  k=5 коллизии, пик уезжал); шаг (е) обобщён в мульти-сид по top-`_SEED_SURAS=8` сурам плотности с
  выбором по реальному difflib-ratio (робастность к ложному пику: истиаза-интро→16:98, рефрен
  Ар-Рахмана размазывает суру). `falign.align()` расщеплён на `emissions()`/`align_verses()`/
  `whole_decode_skeleton()` — один MMS-проход и на локализацию, и на выравнивание. **Валидация:**
  w2v-путь (арабский k=5) 8/9 без регресса (rec7 — известный артефакт устаревших эмиссий);
  forced-самолокализация e2e 7/9 точно (id5/9/10/12/13/14, Ар-Рахман id10 55:1-78 починен), id6/id7
  off-by-one на краю (зачин كٓهيعٓصٓ / хвостовой аят — предел акустики MMS, w2v-дефолт их берёт).
  Офлайн-фикстуры: `work/rec{5,10,14}_mms_emis.npy` + `_id2ch.json`.
- **Шаг 5 (чистка):** ✅ `_inherit_repeats`+`_REPEAT_ZONE_MARGIN` УДАЛЕНЫ (сессия 17, вместе с шагом 4).
  🔴 осталось: разорвать импорт-связь `w2v_*`→`falign` (общие DSP/арабские кирпичи `_HARAKAT`/
  `_snap_bounds`/`_frame_db`/`_sim`/`_collapse_tandem`/`_skeleton`/`_uroman_word` → общий `src/arabic.py`;
  часть уже в `src/alignbricks.py` из инкремента 1).

**Порядок исполнения (безопасный, каждый шаг — рабочее состояние + коммит+пуш):** 2-match_align (✅) →
4-плагин-пакет+лоадер (✅) → 3-MMS-свой-диапазон (✅) → ОСТАЛОСЬ 5-DSP-кирпичи (разрыв `w2v_*`→`falign`).
Кейс rec7 (SPEC-alignment §4 E) чинить
параллельно по спеке — он про качество w2v-возвратов, не про структуру.

**Общие шаги:** матчинг к тексту Корана, возможно и выравнивание — скорее ОБЩИЕ для всех (модуль
`match_align`, импортируется каждым источником). Либо общие только для источников, что выдают
ПОБУКВЕННО (mms/w2v): словесные (google/whisper) → общий словесный матчинг; буквенные → общий
буквенный матчинг + align. Различие источников — ТОЛЬКО в акустике/что выдают, остальное — общий код.

**Директива по w2v: делать С НУЛЯ, сразу нормально** — НЕ «временно беру отсюда, отсюда» (не
наследовать промежуточные результаты MMS). Переписать `w2v` источником с чистого листа под единый
контракт + общий `match_align`.

## ⚠️ Находка 24.07: самоподобие НЕ ловит все возвраты (шаг 1 сложнее, чем казалось)

Тест детекта на самоподобии на ЧИСТОМ w2v (без inherited точек):
- rec7 разрыв 101 (وخلق كل شيء): sim 0.34, n=2, фраза 101:11-13 — **ловит верно** ✓
- rec11 ранний разрыв (~51с): sim 0.42 — ловит ✓
- **rec11 53:53 (кросс-аятный возврат, 447с): sim 0.08 — НЕ ловит** ✗
- خضرا (rec7 99): на чистом w2v разрыва нет / sim низкий — держится на inherited из MMS
**Вывод:** самоподобие ловит МЕЛОДИЧНЫЕ повторы (растянутый распев → высокое самоподобие), но НЕ
все (rec11 53:53 читается иначе, самоподобие низкое). Значит просто убрать `_inherit_repeats` и
заменить самоподобием = РЕГРЕССИЯ rec11/خضرا. Нужны КОМБИНИРОВАННЫЕ сигналы возврата в общем модуле
(самоподобие + акустический декод wav2vec2 + пауза-структура), калиброванные так, чтобы покрыть все
типы БЕЗ ложняков. Это реальная многошаговая работа — НЕ убирать inherit, пока детект не покрывает всё.

## ✅ Де-рискинг 24.07: КАК w2v определяет диапазон САМ (без ASR) — доказано на rec7

Владелец (голос 24.07, жёстко): w2v НЕ использует НИЧЕГО от google/whisper/forced — ни диапазон,
ни windows, ни возвраты. Только своя акустика. Значит нужен «свой распознаватель» из w2v-эмиссий.
Прогон эмпирики (эмиссии в `work/rec7_w2v_emis.npy`, пробы `work/probe_*`):

1. **wav2vec2 (whisperx align-модель = полноценная CTC ASR) отдаёт арабские буквы+харакаты**
   (словарь 51: буквы 6..45, харакаты 46..50, `<pad>`=blank=0). Концепт есть.
2. **Greedy-декод БЕДЕН: 84% кадров = blank** (мелодичный таджвид далёк от обучающей речи модели →
   «молчит» на распевах, буквы в 14% кадров). Декод rec7 = 1053 símb на 255с. Словесный матчинг
   (`align.py`) на нём = **cov 0.0**; char-level k-грамм-матчинг/монотонная цепочка — сура 6
   всплывает как кандидат, но диапазон грубый/шумный. Greedy борется с разреженностью и проигрывает.
3. **✅ РЕШЕНИЕ — CTC-forward-скоринг (не декод!).** Считаем logP(эмиссии | текст-кандидат) CTC-forward'ом
   в лог-пространстве (numpy, БЕЗ GPU, на сохранённых эмиссиях), нормируем на длину меток. CTC
   МОДЕЛИРУЕТ blank'и (не выбрасывает) → разреженность не мешает. Истинное окно = максимум score.
   **rec7 (истина 6:95-103):** norm/char −3.803 (ЛУЧШИЙ) vs 6:95-110 надмн. −3.836, 6:90-103 −4.377,
   6:60-78 −4.383, 2:1-20 −4.523, 6:1-9 −5.859, 6:99-103 подмн. −7.211. **Истинный диапазон
   выигрывает чётко.** Проба — `work/probe_ctc_score.py` (векторизованный CTC-forward по S).

**→ Архитектура независимого w2v (строим):** audio → wav2vec2 эмиссии (вся запись) → (a) грубый
кандидат-регион (greedy-декод + k-граммы → сура/окрестность) → (b) CTC-скоринг скользящих окон
корпуса вокруг кандидата → ТОЧНЫЙ диапазон surah:ayah → (c) force-align этого диапазона (whisperx)
→ границы слов → (d) СВОИ возвраты из своей акустики (см. ниже). Ноль данных от других источников.

### Возвраты w2v из своей акустики (де-рискано на rec7 6:101 «وخلق كل شيء ×2»)
- rec7 6:101: чтец читает وخلق كل شيء ДВАЖДЫ мелодично в дыре 192.5–204.8с (застой на صاحبة 12с —
  жалоба владельца), потом ещё раз чётко. MMS там СЛЕП (0% букв) → forced/inherit промахивались.
- **Дискриминатор (относительный, робастный, НЕ абсолютный порог):** декод дыры (по w2v-эмиссиям,
  арабский согласный скелет) матчит ВПЕРЁДНИЕ слова СИЛЬНО лучше, чем тянутое предыдущее слово →
  чтец читает вперёд (заполняем дыру этими словами), а не тянет мадд. rec7: fwd(وخلق ×2+продолж.)
  =0.409 vs held(صاحبة)=0.045–0.068 vs held(لَهُ)=0.159 → разделение 2.6–6×. Скан всех дыр rec7:
  сработало на 2 реальных зонах (127 وخلق, 68 خضرا), 0 ложняков на обычных словах. Пробы
  `work/scan_w2v_gaps.py`, `work/probe_w2v_repeat.py`.

## ✅ СДЕЛАНО (24-25.07): whisperx выкинут, свой CTC-Viterbi forced-align

Владелец (голос): «зачем wave2vec нужен whisperx? делай сам на себе». WhisperX = обёртка (грузила
wav2vec2-модель + давала CTC-forced-align). Мозг — 100% wav2vec2, но именно whisperx-овская функция
выравнивания ЛОМАЛА мелодичный хвост (rec7: collapsed_words=14, 6:102:10..103:7 в один момент 210.16с;
патч «разложить слипшееся» в w2v_align НЕ помог — клуппинг был внутри whisperx). Реализовано:
- **Модель грузим напрямую через `transformers`** (Wav2Vec2ForCTC/Processor), аудио — `soundfile`
  (`_load_wav`), вход нормализуется feature-экстрактором. `emissions()` считает log-softmax логитов
  сам — один GPU-проход на запись. whisperx полностью убран (в т.ч. из `w2v_repeats` — грузит
  аудио тем же `_load_wav`).
- **`forced_align`: СВОЙ монотонный CTC-Viterbi** (`_ctc_viterbi`): расширенная посл-ть [blank, l0,
  blank, ...], переходы stay/+1/+2 (скип на blank запрещён → каждой метке ≥1 кадр → НЕ схлопывается),
  backpointer'ы → backtrack → кадр на метку. Онсет слова = первый кадр первой метки; слово ВЛАДЕЕТ
  временем до онсета следующего (мадд честно висит на держателе), снап к тишине триммит реальные паузы.
- `gpu_align` W2V-ветка: один проход эмиссий → find_range → forced_align (окна не нужны, путь
  глобален). find_range на нормализованном входе НЕ сломался (rec7 ratio 0.473, диапазон тот же).
- **e2e:** rec7 range 6:95-103 cov 1.0 **collapsed_words=0** (было 14, хвост разложен монотонно),
  rec5 25:63-77 cov 1.0 collapsed=0. forced_align ~1с на CPU. Коммит в main.
- Осталось (в работе): прогон всех 9 записей через celery-флоу + аудит инвариантов (forward_jumps).
  Мелочь: `w2v_range.ayah_start_hints` теперь мёртвый код (окна не нужны) — можно убрать позже.

## Принцип на будущее (записать и держать)

Не строить источник на выходе другого источника. Общее — только в общем модуле, импортом. Тогда
каждая новая идея улучшает КОНКРЕТНЫЙ источник, а не тянет чужие косяки. (Мой прошлый подход —
слой на слой, наследование результатов — привёл к «каше»; это зафиксировано как урок.)
