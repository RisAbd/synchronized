# Заметки — synchronized

## Раунд правок после демо (2026-07-04)

Автор посмотрел прототип (через ngrok) и надиктовал 4 пункта — расшифровка в
`media/audio_2026-07-04_01-15-09.transcript.txt`, детально в [`TZ.md`](./TZ.md) §5:

1. **Источник рядом** — хранить ссылку, для YouTube встроить видео у текста (M2+M5).
2. **Сервис-очередь** — add-by-link + фоновая обработка + статусы + CRUD (новый M7).
3. **Фокус-режим** — крупный текущий аят в фокусе, контекст приглушён, постоянный индикатор. ✅
4. **Mobile-first** — основной кейс телефон. ✅

Сделано в этот заход: **П3+П4** — `web/player.html` переписан в фокус-режим (активный аят
крупно и по центру, соседи приглушены, плавающий бейдж «сура:аят», размеры на `clamp()`/vw,
скролл только внутри потока). Далее: **П1, П2**.

> ⚠️ `player.py` (standalone `_HTML`) пока в старом виде — фокус-режим только в веб-плеере;
> синхронизировать шаблоны при следующем заходе.

## Прогресс

**Сквозной прототип работает** (`run.py`: ingest → asr → align → player). Модули:

- **M1 `quran` ✅** — `quran.py` + `test_quran.py` (11/11). 114 сур / 6236 аятов, нормализация
  арабского, плоский корпус 78 245 токенов с обратным маппингом. Stdlib, без зависимостей.
- **M2 `ingest` ✅ (частично)** — `ingest.py`. Файл — работает. YouTube — упирается в анти-бот
  (см. ниже).
- **M3 `asr` ✅** — `asr.py`. faster-whisper large-v3, GPU, пословные тайминги. На тесте выдал
  более чистую транскрипцию, чем старый Google STT (такбир+Фатиха распознаны точно).
- **M4 `align` ✅ (ядро, v1)** — `align.py`. Биграммные якоря + локальный консенсус по диагонали
  + сегментация + **forward-chain** (наибольшая неубывающая по позиции цепочка) для отсева
  ложняков. **Детект смены суры работает.**
- **M5 `player` ✅ (v1)** — `player.py` → самодостаточный `player.html` (RTL, крупный текст,
  подсветка аята, авто-скролл, тема свет/тьма, мобилка).

### Валидация на записи Аль-Исра (`oM1e8fILtaM`, та самая, что слушает автор)

Прогнали и старый Google STT (`gstt_response.json`), и свою faster-whisper транскрипцию:

- Google STT: привязано **90%**, чистый монотон Фатиха → **17:1 → 17:60**.
- Наша ASR: привязано **70%**, Фатиха → **17:1 → 17:53**, тоже чисто.
- Симуляция плеера по таймкодам: 0:05→1:1, 0:45→17:1 (переход), 3:00→17:7, 15:00→17:41,
  18:20→17:53 — синхрон корректный.

Именно здесь застревали в прошлый раз (difflib) — теперь ядро выдаёт верный результат,
включая **смену суры Фатиха→Аль-Исра**.

### Что дальше (по приоритету)

1. Поднять покрытие нашей ASR (сейчас 70% vs 90% у Google) — больше слов/биграмм: попробовать
   `condition_on_previous_text`, отключить агрессивный VAD, склейку коротких whisper-слов.
2. Пословная подсветка (сейчас по аятам) — есть пословные тайминги.
3. Кэш готовых sync-map для «любимых» видео (в машине не пересчитывать).
4. Live-режим (M6).
5. Тесты на align (сейчас валидируется прогоном; добавить проверки на сэмплах).

### Известные quirks

- **Басмала вшита в 1-й аят** каждой суры (кроме 9-й Тавбы) в этой `quran.db`. На align не
  влияет (консенсус отрабатывает повторы), в плеере басмала показывается в блоке аята 1 —
  приемлемо; при желании вынести отдельной строкой.
- **YouTube-скачивание**: `yt-dlp 2025.07.21` даёт «content not available on this app»;
  куки из firefox дают HTTP 413 (слишком много куки, 1044 шт). Обход: обновить yt-dlp до
  свежее, либо экспортнуть только youtube.com-куки в файл и `--cookies file`. Для локальных
  файлов всё работает.

## Исходная постановка

- Голосовая: `audio_2026-07-04_00-24-25.ogg` (8:40).
- Расшифровка: `audio_2026-07-04_00-24-25.transcript.txt` (whisper large-v3, GPU, ru).
- Суть выжата в `README.md` и `TZ.md`.

## Разбор старых наработок — `../speech-to-text-python`

Автор ссылался на этот проект в аудио. Прошлая попытка той же идеи. Что там есть и что переиспользуем:

| Файл | Что делает | Вывод |
|---|---|---|
| `gcloud-speech-app.py` | ASR через **Google Cloud Speech-to-Text** (арабский, word time offsets) → `gstt_response.json`. Заливка в GCS bucket. | Облако + креды + bucket. **Заменяем на локальный faster-whisper** (цель «локально/легко»). |
| `find-text-in-qurandb.py` | Ядро сопоставления. Снимает харакаты, `difflib.SequenceMatcher` матчит отрезки транскрипции против окон аятов, рейтингует. | **Здесь и застряли.** difflib слаб; нет обработки смен места. Переиспользовать: нормализацию (`text_without_hareke`, `ARABIC_LETTERS`), идею окон аятов. Апгрейд алгоритма → M4. |
| `transriptions-test.py` | Детект пауз по длительностям/разрывам слов → нарезка чтения на куски. | Полезно для сегментации в M4 (границы аятов/пауз). |
| `quran.db` | SQLite: `surahs`, `surah_verses` (текст с харакатами), `surah_verse_notes`. Источник: `RisAbd/quran-data` (репо автора). | **Переиспользуем как есть** — основа M1. |
| `surahs.json` | Тот же Коран в JSON. | Альтернатива БД. |
| `research.txt` | Ссылки: fuzzy string matching, Elasticsearch fuzziness, `minineedle` (Needleman-Wunsch). | Направление для M4: настоящий sequence alignment вместо difflib. `minineedle`/`miniseq` уже в requirements. |
| `gcloud-speech-data/*/gstt_response.json` | Готовые ASR-ответы с пословными таймингами по нескольким записям (aswailis_isra, ve_ibadurrahman, kuran_shf_19/20, meryem…). | **Ценно:** тестовые данные для разработки M4 **офлайн**, без прогона ASR. Формат: `results[].alternatives[0].words[]{word,startTime,endTime}`. |
| `aswailis_playlist/*.mp4` | Записи чтений Юнуса Асвейлиса (тарауих и др.) — как раз кейс со сменами сур. | Реальный материал для тестов фазы 3 (переходы). |

## Ключевые решения

1. **ASR: локальный faster-whisper** вместо Google Cloud STT. Уже проверено рабочим на этой машине.
2. **Показываем оригинал, не транскрипцию.** ASR — только для поиска места и таймингов.
3. **Контракт `sync-map.json`** развязывает offline и live от плеера.
4. **Плеер — простая HTML-страница** (без PyQt), работает и на телефоне.
5. Разработку ядра (M4) ведём на существующих `gstt_response.json` — не жжём время на ASR в цикле.

## Технические заметки по транскрипции (fix cuDNN)

faster-whisper на GPU падал: `Unable to load libcudnn_ops.so.9` (exit 134). Лечится подстановкой
pip-путей к cuDNN/cuBLAS перед запуском:

```bash
export LD_LIBRARY_PATH="$HOME/.local/lib/python3.12/site-packages/nvidia/cudnn/lib:$HOME/.local/lib/python3.12/site-packages/nvidia/cublas/lib:$LD_LIBRARY_PATH"
```

`large-v3` float16 влезает в 6 ГБ RTX 3060; ~9 мин аудио считается меньше минуты.
(Сохранено также в личный skill `audio-task`.)

## Замечания / риски

- Качество whisper на **кораническом арабском** не проверено — снять на сэмплах в фазе 2;
  возможно понадобится спец-модель. Для align, вероятно, хватит и приблизительной транскрипции.
- В старом ASR транскрипция без харакатов и с ошибками — именно поэтому нужен устойчивый
  fuzzy-alignment, а не точное совпадение.
- Смены сур в тарауих — главная нетривиальность (M4.C), под неё уже есть реальные записи.
