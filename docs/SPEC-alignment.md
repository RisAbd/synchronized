# SPEC — синхрон подсветки с чтением (возвраты, дрейф, инвариант чтеца)

> **Зачем этот файл.** П8 (возвраты чтеца) и вообще «подсветка не совпадает с тем, что реально
> читается» — самый рецидивирующий баг проекта. Чиним точечно → ломается в другом месте → «каша».
> Владелец (голос 24.07): чинить **инкрементально по кейсам, не замазывать**. Правило работы:
>
> **Всплыл баг возвратов / несоответствия подсветки → идём СЮДА. Вытаскиваем конкретный случай
> (запись + таймкод) → смотрим, каким кейсом он покрыт → если не покрыт, ДОБАВЛЯЕМ кейс в каталог
> и правим код ПОД спеку (не наоборот). Каждый фикс ссылается на кейс. Спека — живая.**
>
> Держи файл в актуальном виде рядом с кодом (`src/falign.py`, `service/recitations/pipeline.py`).

---

## 1. Инвариант чтеца (фундаментальное правило)

Чтец во времени идёт по тексту так и только так:

- **вперёд ровно на одно слово** (нормальное чтение), ЛИБО
- **назад — возврат/перечитка** на любое расстояние (одно слово, несколько, целый аят и дальше;
  чтецы так делают постоянно), ЛИБО
- **держание** текущего слова (мадд — долгая гласная; пауза; вдох).

**Резкого прыжка ВПЕРЁД через слова (пропуск) физически не бывает.** Если подсветка перепрыгнула
через непрочитанные слова — это баг привязки, всегда.

Из инварианта следуют ДВЕ независимые оси отказа (важно их не путать — раньше путали):

| Ось | Что нарушено | Ловит | Проявление у владельца |
|-----|--------------|-------|------------------------|
| **A. Последовательность** | порядок слов по времени: сосед по времени прыгнул на +2 и дальше по тексту | `alignment_invariants` → `forward_jumps` | подсветка скачет вперёд через слова |
| **B. Привязка по времени** | слово стоит там, где акустика его НЕ подтверждает (время врёт, хотя порядок цел) | `acoustic_health` (§3.2) | «текст уезжает вперёд», подсветка обгоняет голос, мелькание, залипание |

Монотонный выравниватель (forced/w2v) по построению НЕ нарушает ось A внутри своего прохода, но
**легко нарушает ось B**: когда чтец возвращается/повторяет, а текст ему не дали переигрывать,
слова прижимаются друг к другу или растекаются по тишине — порядок цел (fj=0), а время врёт.
**fj=0 НЕ значит «привязка верная».** Это и есть корень «каши»: чинили ось A, не замечая ось B.

---

## 2. Термины

- **forced** — MMS-CTC выравниватель (`src/falign.py`), романизует арабский, роняет мадд → ложные дыры,
  но хорошо детектит возвраты по эмиссиям. Диапазон берёт из готового ASR (`_forced_source`) — легаси.
- **w2v** — wav2vec2 + **СВОЙ CTC-Viterbi** (`src/w2v_align.py`, **БЕЗ whisperx**), держит мадд →
  честный coverage. Монотонен, но возвраты выражает **САМ из своей акустики** (`src/w2v_repeats.py`,
  детект по РАСТЯНУТОМУ span, см. кейс D). Диапазон находит сам из акустики (`src/w2v_range.py`), ASR
  не нужен. `_inherit_repeats` из forced **УБРАН**. **Дефолт на всех 9 записях** (сессия 15, 25.07).
- **возврат (rep)** — чтец вернулся назад и перечитал (BACK) или мелодично обогнал вперёд перечиткой
  (FWD); точки `rep=True`. У w2v детектятся своей акустикой (`w2v_repeats`), у forced — `_detect_repeats`.
  В `data` возврат выражается **ДУБЛЕМ точки** во времени (то же слово дважды), не флагом (build_data
  роняет `rep`).
- **зона возврата** — непрерывный диапазон слов в порядке чтения, чью раскладку сдвинул возврат.
- **эмиссии** — выход MMS перед CTC (`work/rec<id>_emissions.npy`); можно декодить greedy-CTC
  (`falign._greedy_ctc`) → романизованные буквы → сверять со скелетом слова (`falign._skeleton(
  falign._uroman_word(w))`). **Главный инструмент диагноза БЕЗ звука** (владелец: «глянь буквы,
  которые слышатся»).

---

## 3. Автоматические проверки (asserts на КАЖДОМ прогоне)

Владелец (голос 18.07): проверки — алгоритмические, в коде, автоматом на каждом прогоне, не ручные.
Кладутся в `run.metrics`, видны в листе/аудите.

### 3.1. `alignment_invariants(data)` — ось A ✅ реализовано

`service/recitations/pipeline.py`. Плоский индекс слова по канону (`_flat_index`): соседи по времени
должны идти +1 или ≤0, не ≥+2. Пишет `forward_jumps`, `collapsed_words` (слово мелькает <20мс),
детали. Аудит по всем прогонам: `tools/audit_alignment.py`.

### 3.2. `acoustic_health` — ось B 🔴 НЕ реализовано (4 абсолютных сигнала отвергнуты — см. ниже)

Задумка: пометить слова, чья привязка по времени НЕ подтверждается акустикой (стоят в тишине /
не там, где реально читаются), — доля идёт в metrics как объективный индикатор дрейфа при fj=0.
**Статус (сессия 17):** 4 АБСОЛЮТНЫХ пороговых сигнала проверены и отвергнуты (скелет-сверка,
RMS-тишина, громкий-разрыв-RMS, wav2vec2-побуквенная-активность) — ни один не разделяет дрейф от
нормы на всех 9. Уцелел только ОТНОСИТЕЛЬНЫЙ путь (сверка со вторым независимым декодом). НЕ шипить
абсолютный порог — доказанно ложнит на здоровых.

**Проверено 2 наивных подхода — ОБА слишком шумные, чтобы на них гейтить (24.07, прототипы):**

1. **Скелетная сверка** (greedy-CTC декод окна `[t,t_next)` vs `_skeleton(_uroman_word(w))`, `_sim≥0.3`).
   Даже здоровые прогоны rec7 дают health≈0.44–0.56; google (cov 0.851) — худший 0.30. Короткие
   романизованные слова MMS декодятся слишком грязно → нет чистого порога «хорошо/плохо».
2. **RMS-тишина** (доля слов, чей онсет `[t,t+0.3с)` ниже порога речи = floor⁺¹⁵퍼 centile+10dB).
   Порог шумового пола НЕ сравним между записями: rec5 (здоровая, но тихий фон/паузы) → speech
   лишь 22% кадров → drift 0.43; rec10 → 2% кадров → drift 0.99. Медленное чтение с паузами
   неотличимо от дрейфа по абсолютному порогу. (Та же причина, по которой VAD отключён в ASR —
   на разнородном YouTube-таджвиде сегментация речь/тишина сама по себе ненадёжна.)

**Вывод:** абсолютной пороговой метрики оси B, сравнимой между записями, из этих сигналов НЕ выходит.
Нужен ОТНОСИТЕЛЬНЫЙ, привязанный к акустике САМОЙ записи сигнал. Кандидаты (не проверены):
- сравнивать онсеты прогона с **онсетами forced (MMS)** той же записи — forced даёт акустически
  обоснованные позиции слов; расхождение прогон↔forced ТАМ, где forced в здоровой зоне, = дрейф
  прогона (но forced сам дрейфует на кейсе E — нужен фильтр здоровья зоны, как в `_detect_repeats`);
- эмиссионная (не RMS) тишина — max-softmax эмиссий MMS в окне (низкая уверенность = нет речи),
  устойчивее к фоновому шуму, чем RMS (не проверено).

**Проверен 3-й подход — «громкий разрыв» (24.07, ближе всего к цели):** разрыв в таймлайне ≥3с между
`t_end` слова и `t` следующего, где RMS в разрыве показывает речь (≥50% кадров). Тихая легитимная
пауза НЕ попадает (она тихая) → 0 ложных на здоровых rec5/6/9/10; ловит реальные разрывы rec11
(53:53 зона возврата), rec12. НО на rec7 пропустил ГЛАВНЫЙ разрыв 192–204с (тот самый وخلق):
RMS-порог floor+10дБ на шумном фоне rec7 счёл громкий распев «недостаточно речью». Опять хрупкость
RMS-порога.

**Гипотеза «ИТОГ по оси B» (24.07):** надёжный сигнал «чтец читает, подсветка стоит» — НЕ RMS, а
**буквенная активность wav2vec2** (в ОДНОМ разрыве rec7 192-204 wav2vec2 дал 22% кадров с буквами,
где MMS и RMS-порог соврали). Идея: `loud_gaps`/`frozen_sec` в metrics на wav2vec2-активности.

**Проверен 4-й подход — wav2vec2-буквенная-активность ПОБУКВЕННО (25.07, сессия 17, прототип на ВСЕХ
9 записях): ❌ НЕ РАЗДЕЛЯЕТ, НЕ шипить.** Сигнал без порога: кадр «активен буквой», если argmax(E) —
базовая согласная (как greedy). Мера заморозки: держание длинного span при буквенной активности сверх
собственных букв слова. **Результат (`work/proto_acoustic_health.py`/`proto_ah_sep.py`, эмиссии
`work/rec*_w2v_emis.npy` + живой w2v word_timeline): распределения держаний rec7 (дрейф) и здоровых
rec5/6 СТАТИСТИЧЕСКИ ИДЕНТИЧНЫ.** По держаниям ≥4с: act% медиана rec7=0.075 vs rec5=0.079, rec6=0.077;
p90 rec7=0.092 vs rec5=0.095; max act_s rec7=0.52 vs rec5=0.60. **rec7 даже НИЖЕ здоровых** — любой
порог, ловящий rec7, зальёт ложняками rec5/6. Причина: у ЭТОГО чтеца длинный разрежённый мадд на
рифме-концовке (-ِيًّا/-ًّا, وَيَعْقُوبَ 6.2с, أُولَٰئِكَ 7.0с) — НОРМА стиля, акустически неотличимая
от «дрейф-держания». Единичный разрыв 192-204 (на котором строилась гипотеза «ИТОГ») — не признак
класса: как ГЛОБАЛЬНАЯ побуквенная метрика сигнал сливается. **Подтверждает вывод стр. 88 и
РАСШИРЯЕТ его на wav2vec2:** абсолютной/побуквенной пороговой метрики оси B из активности эмиссий НЕ
выходит — ни MMS, ни RMS, ни wav2vec2.

**Что остаётся (единственный неопровергнутый кандидат) — ОТНОСИТЕЛЬНЫЙ сигнал, не абсолютный порог:**
сверять онсеты прогона со ВТОРЫМ независимым акустическим декодом того же окна (онсеты forced-MMS той
же записи в ЗДОРОВОЙ зоне, либо аудио-самоподобие MFCC для повтора-фразы) — расхождение ТАМ, где второй
источник уверен = дрейф. Это МЕТРИКА (читает оба источника для диагностики), не наследование данных в
таймлайн — независимость источников не нарушает. Дорого и не быстро; отдельным заходом.

**Пока метрики нет — ось B диагностируем ВРУЧНУЮ по процессу §5** (декод эмиссий окнами). Не шипить
хрупкую эвристику: ложные тревоги на здоровых записях = та же «каша», от которой уходим (подтверждено
4 отвергнутыми подходами — не изобретать 5-й абсолютный порог).

---

## 4. Каталог кейсов

Каждый: **симптом → акустическая сигнатура → правило → текущая обработка → тест (запись+таймкод) → статус.**

### Кейс A — нормальное чтение вперёд ✅
- **Симптом:** норма, подсветка едет по одному слову.
- **Правило:** сосед по времени = +1 по индексу.
- **Обработка:** любой выравниватель.
- **Статус:** ок (эталон инварианта).

### Кейс B — одиночный возврат внутри аята ✅
- **Симптом:** чтец перечитал слово/несколько в том же аяте.
- **Акустика:** дыра в монотонном таймлайне + речь (RMS) + greedy-CTC-декод эмиссий совпадает со
  «назадними» словами эталона лучше, чем с «вперёдними».
- **Правило:** вклеить назадние точки (`rep=True`), фронт не трогать (bsearch по t).
- **Обработка:** `falign._detect_repeats` (Левенштейн ≥`_REPEAT_MIN_SIM`, tandem-collapse от удвоенного
  декода, добор перечитки `rb=max(rb,i)`).
- **Тест:** rec7 خضرا 6:99, ~1:28 (wi13→wi11→wi12→wi14). **Статус:** ок.

### Кейс C — держание на мадде (долгая гласная) ✅
- **Симптом:** долгий مادّ, forced роняет слово → дыра → залипание/скомканность.
- **Акустика:** одна held-гласная тянется секунды, согласных нет.
- **Правило:** не отсеивать stuffed-слова, а растянуть скомканный прогон по дыре; пред. слово держит
  фокус сквозь мадд.
- **Обработка:** `falign._stuffed_runs` + `_respace_stuffed` (опт-аут `SYNC_FALIGN_RESPACE=0`).
- **Тест:** rec11 Ан-Наджм 53:3→4 (الْهَوَىٰ держится ~4с). **Статус:** ок.

### Кейс D — кросс-аятный возврат (перечитка через границы аятов) ✅ закрыто 24.07
- **Симптом (w2v):** чтец вернулся и перечитал через границу аята; монотонный w2v затолкал слова
  СЛЕДУЮЩЕГО аята раньше по времени, чем перечитанные слова предыдущего → зигзаг по времени (ось A
  на w2v: fj>0).
- **Акустика:** зона расхождения forced↔w2v по времени (>1с) непрерывна и пересекает границу аята.
- **Правило:** наследуя возвраты из forced на w2v, замещать forced-точками ВСЮ непрерывную зону
  возврата в порядке чтения — **через границы аятов**, — а не по-аятно.
- **Обработка:** `pipeline._inherit_repeats` переписан на **flat-кластеры**: rep∪diverge-слова
  собираются в плоском порядке чтения, бьются на непрерывные кластеры (разрыв позиции >1 разделяет
  разные возвраты — честный w2v между ними не трогаем), каждый кластер `[lo..hi]` замещается forced.
- **Тест:** rec11 Ан-Наджм 53:53→(возврат 53:53)→54→55. Было w2v fj=3 (443→446→456 зигзаг),
  стало fj=0, время монотонно, возврат 53:53 выражен, cov=0.774. **Статус:** закрыто.
  (Раньше обходили дефолтом на forced; теперь w2v сам чист и выбирается по `min forward_jumps`.)
- **⚠️ ОБНОВЛЕНО сессия 15 (25.07): механизм ПЕРЕПИСАН.** `_inherit_repeats` (наследование зоны
  возврата из forced на w2v) **УБРАН** — нарушал независимость. Теперь w2v детектит возвраты САМ из
  своей акустики (`w2v_repeats.detect`): монотонный Viterbi дыр не оставляет → возврат = span
  слова-остановки РАСТЯНУТ (не дыра). Внутри длинного span — акустический разбор greedy-CTC декода vs
  скелеты: FWD (мелодичный обгон [i+1..j]) / BACK (перечитка назад [ra..i]). Пост-гард: группа
  принимается, только если НЕ добавляет прыжка вперёд (fj≤base) → кросс-аятный зигзаг rec11 гасится
  отбраковкой групп, а не наследованием. Результат: fj=0 на всех 9, w2v дефолт. Каталог кейса верен,
  реализация сменилась с `pipeline._inherit_repeats` (мёртвый код, к удалению) на `w2v_repeats`.

### Кейс E — похожие/повторяющиеся аяты + тихие провалы в хвосте → дрейф по времени 🔴 ОТКРЫТО
- **Симптом (владелец, голос 24.07, rec7 хвост после 2:00):** «текст уезжает вперёд, чтец с
  повторениями читает, похожие аяты идут, распознавание чуть-чуть вперёд бежит». Подсветка обгоняет
  голос, проносится через аят в почти-тишине.
- **Акустика (декод rec7 хвоста, эмиссии `work/rec7_emissions.npy`):**
  - 118–186с — сплошная речь, декод содержательный.
  - **186–204с — почти пусто** (нет чётких согласных): тихий провал/пауза чтеца.
  - w2v при этом расставил аят **102:0–15 на 188–202с** — прямо в тихом провале → подсветка
    проносится через весь 102, пока чтец молчит/читает не там.
  - 204–234 речь возобновляется (103), 234–242 снова провал, 242–256 речь.
  - Похожие аяты: 101 и 102 оба про «بديع السماوات والأرض / خلق كل شيء» → ASR/выравниватель путают,
    какой именно аят звучит.
- **Почему не ловится:** `forward_jumps=0` на всех прогонах rec7 — порядок слов цел. Это **ось B**
  (привязка по времени), а не A. Текущих автопроверок оси B НЕТ.
- **Уточнение механизма (24.07, разбор кросс-аятной data):** в хвосте w2v ВСЕ слова — РЕАЛЬНЫЕ якоря
  (с `t_end`), НЕ интерполяция. То есть w2v акустически выровнял каждое слово; проблема не в «размазке
  интерполяции по паузе». Между якорями есть БОЛЬШИЕ разрывы: 101:10@191.9с → 101:11@204.8с = **13с
  разрыв** с пустым декодом; 101:2@175.4→101:3@181.1 = 5.7с; 103:5@243.2→103:6@249.2 = 6с. В такие
  разрывы подсветка не «уезжает вперёд», а **ЗАЛИПАет** на последнем слове до его `t_end`, потом висит
  до следующего якоря. Симптом «текст уезжает» скорее возникает, если якорь следующего слова
  оказался РАНЬШЕ реального (слово выровнено с чужим похожим вхождением — похожие аяты 101/102).
- **ВЕРДИКТ ВЛАДЕЛЬЦА ПО СЛУХУ (24.07, rec7 3:12–3:24, разрыв на 101:10):** чтец НЕ молчит — он
  читает **وَخَلَقَ كُلَّ شَيْءٍ (101:11-13) ДВАЖДЫ**, потом переходит на **وَهُوَ بِكُلِّ شَيْءٍ
  عَلِيمٌ (101:14-17)**; с 3:24 (204с) всё снова синхронно. То есть это **несматченный ПОВТОР** —
  подгипотеза 2. Highlight залип на 101:10 (صاحبة) 13с вместо того, чтобы вести перечитку 101:11-13.
- **ПОДТВЕРЖДЁННЫЙ КОРЕНЬ (акустика, 24.07):** в разрыве 192–204с **RMS = −17.6 dB (ГРОМЧЕ всех
  прочих окон)** — чтец активно читает; но **letter_mass эмиссий MMS = 0.011** (норма 0.03–0.05),
  greedy-CTC декод ПУСТОЙ. **MMS (обучен на обычной речи) не выдаёт согласных на распевной
  мелодичной перечитке таджвида** — громко, но модель «молчит». `_detect_repeats` физически не мог
  найти повтор: нечего матчить. Это НЕ тюнинг порогов — это предел MMS-подхода к П8 на распеве.
  (Именно поэтому мадд-валидация здоровья зоны 18.07 и не помогла, и любой MMS-детект тут слеп.)
- **Правило/направление фикса (к реализации):**
  1. **Детект «громкого разрыва»:** разрыв в таймлайне (≥~3с между t_end слова и t следующего),
     где RMS показывает РЕЧЬ (громко), но MMS letter_mass ~0 (декод пуст) = чтец читает распевную
     перечитку, которую MMS не берёт. Highlight НЕ должен стоять — там идёт перечитка соседних слов.
  2. **Не-MMS сигнал для повтора — ПРОВЕРЕНО 24.07, РАБОТАЕТ:** прогон whisperx-wav2vec2 (ar) по
     разрыву 190–205с rec7, где MMS даёт 0% букв: wav2vec2 даёт **18–22% кадров с буквами**, и в
     декоде ВИДНО وَخلَق (وخلق — та самая перечитка). wav2vec2 держит распев/мадд → «слышит» то, к
     чему MMS глух. **Фикс:** детект возврата (логика `_detect_repeats`) на wav2vec2-эмиссиях, хотя
     бы в громких разрывах где MMS пуст; матч декода к эталонным словам → вставка точек перечитки.
     (Альтернатива/дополнение — самоподобие аудио: повтор фразы = похожие MFCC-сегменты подряд.)
  3. **Различать (по слуху владельца):** одно слово, повторённое N раз (напр. أُنظُرُوا) — держать
     на нём ДОПУСТИМО; многословная фраза, повторённая с застоем прогресса (этот кейс) — НЕ норм,
     нужен реальный проход подсветки по перечитке.
- **Обработка:** описанный фикс (не-MMS детект повтора) РЕАЛИЗОВАН в `w2v_repeats.detect` (сессия 15) —
  span-детект FWD/BACK на w2v-эмиссиях. **Не замазывать заполнением** (пост-гард `fill_forward_jumps`
  был замазкой, откачен 18.07 — рисовал, будто пропущенные слова читаются; не воскрешать).
- **Тест:** rec7 Аль-Анам 6:101, разрыв 192–204с (وخلق كل شيء ×2 → وهو بكل شيء عليم). Владелец: таких
  мест «хватает» — где чтец читает несколько раз, прогресс стоит.

- **🔴 АКТИВНО НА W2V (жалоба владельца tg_1786, 24.07 23:54, слушал ИМЕННО W2V-версию):** «в конце
  Энам (rec7), последняя ~минута, текст уезжает вперёд, чтец читает с повторениями, аяты похожие →
  подсветка бежит вперёд». То есть после сессии 15 (w2v-дефолт, свой `w2v_repeats`) кейс E на слух
  ВСЁ ЕЩЁ воспроизводится. session-15 отмечал, что `w2v_repeats` ловит 6:101 صاحبة как FWD (وخلق كل
  شيء распев ×2) — но владелец слышит рассинхрон. Гипотезы к проверке (диагностика offline по
  `work/rec7_w2v_emis.npy` + текущая `run.data` w2v на rec7):
  1. FWD-детект 6:101 срабатывает, но раскладывает перечитку НЕ туда (границы фразы/число копий), или
  2. проблема НЕ на 6:101, а ниже в хвосте (6:102/103 — похожие «بديع/خلق» аяты, диапазон-матч увёл
     слово на чужое похожее вхождение → t раньше реального → «текст уезжает вперёд»), ось B.
  **Следующий шаг (сессия 16): вытащить текущий w2v word_timeline rec7 после 2:00, посчитать fj,
  декодить эмиссии окнами по хвосту, сверить со скелетами 6:101-103, определить — сработал ли FWD и
  где именно рассинхрон.** По результату — правило в `w2v_repeats` (не в MMS).

- **✅ ДИАГНОСТИКА ПРОВЕДЕНА (сессия 16, 25.07, offline по `work/rec7_w2v_emis.npy` + текущая
  `run.data` w2v; пробы `work/probe_rec7_*.py`):**
  - fj=0, reps=8, cov=0.927, диапазон 6:95-103 — верны. Ось A цела; проблема — ось B (время врёт).
  - **FWD-возврат СРАБОТАЛ и в целом верно:** وخلق كل شيء прочитан ×2 (первый распевный проход
    декод ~193-196с «خلمرك/مركللشرن», чёткий второй ~197-198с пик sim=0.50); группа `6:101:11-14`
    вставлена на 193.28-196.89, реальная копия 196.92-200.43. Раньше (до сессии 15) этот повтор
    вообще не ловился (MMS слеп) → это РЕАЛЬНОЕ улучшение.
  - **❌ ГИПОТЕЗА ДЕФЕКТ №1 (FWD захватывает лишний وهو(14)) — ПРОВЕРЕНА И ОТКЛОНЕНА (не повторять).**
    Идея была: реальная перечитка только وخلق كل شيء (11-13), а وهو(14, скелет `ه`) — продолжение,
    цепляется и показывается раньше голоса. Реализовал трим хвостовых слов FWD-фразы по `_sim` и
    прогнал **before/after офлайн на всех 9 реках** (`work/test_trim_ba.py`): трим — **полный no-op
    везде** (off==on). Причина принципиальная: greedy берёт `j`=argmax sim, и результат показывает, что
    каждое хвостовое слово добавляет >0.05 к sim → границы фразы **акустически обоснованы**. Декод
    span صаحبة = «ققخلمركللشرنيه» реально заканчивается на `ه` — модель ЭМИТИТ وهو в span (первый
    распевный проход захватывает и его). Значит FWD-раскладка не врёт, лишнего слова нет. Трим откачен
    (`src/w2v_repeats.py` чист). **Урок:** не приписывать «over-reach» без before/after на акустике.
  - **ДЕФЕКТ №2 (предел акустики, НЕ быстрый фикс): дрейф ±1-2с в распевных разрежённых зонах.**
    На тяжёлом мелодичном таджвиде w2v декодит ОЧЕНЬ разрежённо (зона أنى 180.8-186.4с — почти пустой
    декод «م/ن/ميم/ннه»): онсеты слов ненадёжны → короткое слово держится через мелизму, следующие
    крамятся (أنى `ن` держится 5.54с; конец 101: عليم(17) поставлен 205.26с, а акустически عليم
    звучит 202-204с, и с ~204с уже идёт 102 «خالق كل شيء» → подсветка конца 101 запаздывает ~2с).
    Это ось B от разрежённости эмиссий на распеве — та же природа, что «MMS слеп на мелодике».
    Требует НАСТОЯЩЕГО решения (не порога): кандидаты — (а) детект аномально длинного держания
    короткого слова + перераздача по декоду следующих слов (обобщить `falign._respace_stuffed`,
    кейс C, на w2v — сейчас его у w2v НЕТ); (б) слабые буквы-якоря внутри распева как мягкие точки.
  - **❌ ГИПОТЕЗА «softmax-масса букв как побуквенный сигнал онсета» — ПРОВЕРЕНА И ОТКЛОНЕНА (сессия
    17, прототип на всех 9, `work/proto_acoustic_health.py`+`proto_ah_sep.py`).** Держания rec7
    (дрейф) статистически ИДЕНТИЧНЫ здоровым rec5/6 по буквенной активности (act% медиана 0.075 vs
    0.079/0.077; rec7 даже ниже) — разрежённый мадд на рифме у ЭТОГО чтеца норма, неотличим от
    «дрейф-держания». Значит перераздача онсетов по softmax-массе в зоне أنى невозможна: разделяющего
    сигнала НЕТ (см. §3.2, 4-й отвергнутый подход). Не приписывать «где реально буква» абсолютному
    порогу активности. **Уцелевший путь — ОТНОСИТЕЛЬНЫЙ:** сверка со вторым независимым декодом
    (forced-MMS-онсеты в здоровой зоне / MFCC-самоподобие для повтора-фразы), не абсолютный порог.
  - **Статус:** открыто, БЕЗ быстрого фикса. Дефект №1 ОТКЛОНЁН (акустика оправдывает FWD-границы).
    Дефект №2 (дрейф ±1-2с на распеве) — предел абсолютной акустики: и метрика оси B, и фикс упираются
    в отсутствие абсолютного сигнала; нужен относительный (2-й источник), дорого, отдельным заходом.
    Ниже — прежняя формулировка направления (respace), НО она требует того же несуществующего сигнала:
    (кейс C) на w2v — детект аномально длинного держания короткого слова + перераздача по декоду
    следующих слов; НО на rec7 зона أنى 180-186 почти ПУСТА в декоде (перераздавать не по чему) →
    сначала нужен НАДЁЖНЫЙ сигнал «где реально слово» в тихом распеве (кандидат: не greedy-argmax, а
    softmax-масса букв по кадрам как мягкие якоря). Глубокая кор-логика, отдельным честным заходом.
    **Не патчить заполнением.** FWD-возврат وخلق كل شيء ×2 — работает (реальное улучшение сессии 15).

### Кейс F — суры-рефрены (повтор одинаковых аятов) ✅ закрыто 25.07
- **Симптом (w2v):** сура с рефреном (Ар-Рахман «فبأي آلاء ربكما تكذبان» ×31), w2v ошибался НЕ на
  выравнивании, а на **выборе ДИАПАЗОНА** (`find_range`): рефрен путал ОДНО глобальное difflib-
  выравнивание декода к региону → покрытие по суре 55 ≈0 → префикс-макс уводил границу в соседа
  (55:77..56:39). Низкий coverage — следствие неверного диапазона.
- **Правило:** при повторяющихся сурах не полагаться на одно глобальное выравнивание; проверять
  кандидат «целая сура пика» по РЕАЛЬНОМУ difflib-ratio.
- **Обработка:** `w2v_range.find_range` шаг (е) — второй сид = RAW целая сура пика, выбор окна по
  МАКСИМАЛЬНОМУ реальному difflib-ratio (истина 55:1-78 имеет ratio 0.32 = максимум; частичные чтения
  не задеты — у целой суры ratio ниже). Плюс в `w2v_align` невалидные t_end (≤t) удаляются.
- **Тест:** rec10 Ар-Рахман, диапазон 55:1-78, cov=0.998, fj=0, reps=4. **Статус:** закрыто
  (офлайн 9/9 диапазонов, e2e GPU подтверждён). Стенд: `work/exp_findrange.py`.

### Кейс G — межсурные/межаятные прыжки ASR (не выравнивание) 🟡
- **Симптом:** google/whisper распознают НЕ тот диапазон аятов (убежали на похожем/следующем), из-за
  чего исходный диапазон для forced/w2v кривой.
- **Правило:** это дефект ASR/диапазона, а не выравнивателя; лечится на уровне выбора диапазона, не
  в falign.
- **Статус:** отдельная линия, не в П8.

---

## 5. Процесс (как пользоваться спекой на каждом баге)

1. **Вытащи случай:** запись + таймкод от владельца → `docker compose exec -T worker python
   manage.py shell` → достань forced/w2v word_timeline в зоне, посчитай `alignment_invariants`.
2. **Определи ось:** fj>0 в зоне → ось A (кейсы B/D). fj=0, но «текст уезжает/обгоняет» → ось B (кейс E).
3. **Декодь акустику** (без звука): `work/rec<id>_emissions.npy` (или посчитай заново),
   `falign._greedy_ctc` окнами → сверь со скелетами слов зоны. Смотри, что РЕАЛЬНО читается где.
4. **Сопоставь с каталогом §4.** Покрыт кейсом → чини по его правилу. Не покрыт → **добавь новый кейс**
   (симптом/акустика/правило/тест/статус) и правь код под правило.
5. **Проверь регрессии:** перегони/переприменённые прогоны, `tools/audit_alignment.py` — fj и (когда
   будет) acoustic_health по ВСЕМ записям; здоровые не должны деградировать.
6. **Обнови статус кейса** здесь + запись в BACKLOG/CLAUDE.md. Коммит+пуш.

**Грабли (не наступать):**
- Прерывание shell по таймауту оставляет осиротевший `gpu_align`, держит VRAM → CUDA OOM следующему.
  Чистить: `kill` gpu_align в контейнере (grep `/proc/*/cmdline`). w2v перегонять ПО ОДНОМУ.
- `build_data` (`src/player.py`) копирует только t/t_end/surah/ayah/wi — роняет `rep`/прочие флаги.
  Возврат в data выражается ДУБЛЕМ точки (то же слово дважды по времени), не флагом.
- Не воскрешать `fill_forward_jumps` (замазка).
