# Django-сервис synchronized (M7)
Django>=5,<7
gunicorn
whitenoise
python-dotenv
celery
redis
# БД — SQLite (bind-mount), Postgres выпилен → psycopg2 не нужен (см. docs/BACKLOG)

# конвейер (ядро src/) — распознавание/скачивание
requests
yt-dlp
faster-whisper        # ASR (ctranslate2); в docker-воркере теперь на GPU (см. Dockerfile + compose)

# Google Speech-to-Text (основной распознаватель для арабского) — лёгкие API-клиенты, не ML.
# Ключ через env GOOGLE_APPLICATION_CREDENTIALS (см. src/gstt.py, монтируется из ./creds/).
google-cloud-speech
google-cloud-storage

# forced align (ctc-forced-aligner, MMS-CTC через onnxruntime) — ставим в docker:
# воркер получил GPU, весь конвейер (whisper→forced→w2v) крутится в сервисе, без хостовых костылей.
ctc-forced-aligner
# onnxruntime-GPU: forced align на CUDA (RTX в воркере). ORT 1.20.x — сборка под CUDA 12 +
# cuDNN 9 (тот же стек, что у ct2/whisper/torch). ORT ≥1.27 требует CUDA 13 → НЕ брать.
# Провайдер подхватывается автоматически (falign._session_and_tokenizer).
onnxruntime-gpu==1.20.1
Unidecode             # романизация (uroman-fallback) в ctc-forced-aligner

# ⚠️ CUDA-стек (cuDNN/cuBLAS/cuda-runtime/cufft/curand/nvrtc/nvjitlink) БОЛЬШЕ НЕ пиним здесь —
# его целиком приносит `torch==2.5.1+cu124` (ставится в Dockerfile ПЕРЕД этим файлом): cuBLAS
# 12.4.5.8 + cuDNN 9.1.0.70 (ровно как раньше) + runtime/cufft/curand/nvrtc/nvjitlink 12.4.127
# — единый непротиворечивый набор CUDA 12.4, который обслуживает СРАЗУ torch(wav2vec2/whisperx),
# onnxruntime-gpu 1.20.1 (SONAME .12/.11/.10 совпадают) и ctranslate2 (faster-whisper). Раньше
# nvidia-* пинились тут вручную (часть на 12.9) — при добавлении torch-cu124 это давало конфликт
# резолвера, поэтому пины убраны, стек унифицирован на cu124. LD_LIBRARY_PATH (Dockerfile)
# указывает на те же pip-каталоги nvidia/* — теперь их наполняет torch.
# whisperx (wav2vec2-выравниватель, src/w2v_align.py) ставится в Dockerfile через --no-deps +
# transformers/pandas/nltk (весь его транскрипционный/VAD-хвост с onnxruntime-CPU не нужен и
# конфликтовал бы с onnxruntime-gpu — align() тянет только torch/torchaudio/transformers).
